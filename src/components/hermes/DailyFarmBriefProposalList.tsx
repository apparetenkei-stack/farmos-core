import Link from "next/link";
import type { DailyBriefProposalListUiState } from "../../lib/hermes/hermes_daily_farm_brief_proposal_review_ui_client";

export function formatDailyBriefProposalDate(value:string):string{const date=new Date(value);if(Number.isNaN(date.getTime()))return"日時を確認できません";return new Intl.DateTimeFormat("ja-JP",{timeZone:"Asia/Tokyo",year:"numeric",month:"2-digit",day:"2-digit",hour:"2-digit",minute:"2-digit",second:"2-digit",hourCycle:"h23"}).format(date);}
function statusLabel(value:string):string{return({pending:"確認待ち",approved:"承認済み",rejected:"却下",needs_revision:"修正依頼",applied:"適用済み",expired:"期限切れ"} as Record<string,string>)[value]??"状態を確認できません";}
function expiryLabel(value:string):string{return value==="expired"?"期限切れ":"有効";}
export function DailyFarmBriefProposalList({state}:{state:DailyBriefProposalListUiState}){if(state.state==="authentication_required")return <section><h2>認証が必要です</h2><p>認証が必要です。</p></section>;if(state.state==="access_forbidden")return <section><h2>権限がありません</h2><p>この画面を表示する権限がありません。</p></section>;if(state.state==="unavailable")return <section><h2>読み込みエラー</h2><p>現在、確認事項を読み込めません。</p></section>;const proposals=state.response.proposals;return <><section aria-labelledby="proposal-summary"><h2 id="proposal-summary">確認候補</h2><p>Proposal件数: {proposals.length}</p>{proposals.length===0?<p>現在表示できる確認事項はありません。</p>:<div>{proposals.map((proposal)=><article key={proposal.proposal_ref}><header><h3>{proposal.title}</h3><p>状態: {statusLabel(proposal.status)} / リスク: {proposal.risk_label}</p></header><p>{proposal.summary}</p><dl><dt>情報源</dt><dd>{proposal.source_kind_label}</dd><dt>作成日時</dt><dd>{formatDailyBriefProposalDate(proposal.created_at)}</dd><dt>期限</dt><dd>{formatDailyBriefProposalDate(proposal.expires_at)}</dd><dt>期限状態</dt><dd>{expiryLabel(proposal.expiry_state)}</dd><dt>人による確認</dt><dd>{proposal.requires_human_review?"管理者の確認が必要":"管理者の確認は不要"}</dd><dt>営農データへの反映</dt><dd>{proposal.proposal_apply_performed?"営農データへ反映済み":"営農データには反映されていません"}</dd></dl><p><Link href={`/proposals/daily-brief/${proposal.proposal_ref}`}>{proposal.title}の確認内容を見る</Link></p></article>)}</div>}</section></>;}
