import Link from "next/link";
import type { DailyBriefProposalDetailUiState } from "../../lib/hermes/hermes_daily_farm_brief_proposal_review_ui_client";
import { formatDailyBriefProposalDate } from "./DailyFarmBriefProposalList";
import { DailyFarmBriefProposalReviewControls } from "./DailyFarmBriefProposalReviewControls";
function statusLabel(value:string):string{return({pending:"確認待ち",approved:"承認済み",rejected:"却下",needs_revision:"修正依頼",applied:"適用済み",expired:"期限切れ"} as Record<string,string>)[value]??"状態を確認できません";}
export function DailyFarmBriefProposalDetail({state,reviewControlsEnabled=true}:{state:DailyBriefProposalDetailUiState;reviewControlsEnabled?:boolean}){if(state.state==="authentication_required")return <section><h2>認証が必要です</h2><p>認証が必要です。</p></section>;if(state.state==="access_forbidden")return <section><h2>権限がありません</h2><p>この画面を表示する権限がありません。</p></section>;if(state.state==="not_found")return <section><h2>確認事項が見つかりません</h2><p>確認事項が見つかりません。</p></section>;if(state.state==="unavailable")return <section><h2>読み込みエラー</h2><p>現在、確認事項を読み込めません。</p></section>;const proposal=state.response.proposal;return <article><header><h2>{proposal.title}</h2><p>状態: {statusLabel(proposal.status)} / リスク: {proposal.risk_label}</p></header><dl><dt>確認理由</dt><dd>{proposal.reason}</dd><dt>対象</dt><dd>{proposal.target_display}</dd><dt>作業種別</dt><dd>{proposal.work_type_label}</dd><dt>根拠</dt><dd>{proposal.basis}</dd><dt>現在</dt><dd>{proposal.before}</dd><dt>対応</dt><dd>{proposal.after}</dd><dt>対象業務日</dt><dd>{proposal.source_business_date}</dd><dt>参照版</dt><dd>{proposal.source_version}</dd><dt>作成日時</dt><dd>{formatDailyBriefProposalDate(proposal.created_at)}</dd><dt>更新日時</dt><dd>{formatDailyBriefProposalDate(proposal.updated_at)}</dd><dt>期限</dt><dd>{formatDailyBriefProposalDate(proposal.expires_at)}</dd><dt>期限状態</dt><dd>{proposal.expiry_state==="expired"?"期限切れ":"有効"}</dd><dt>人による確認</dt><dd>{proposal.requires_human_review?"管理者の確認が必要":"管理者の確認は不要"}</dd><dt>適用操作</dt><dd>{proposal.proposal_apply_ready?"適用操作を確認できます":"適用操作は利用できません"}</dd><dt>営農データへの反映</dt><dd>{proposal.proposal_apply_performed?"営農データへ反映済み":"営農データには反映されていません"}</dd></dl>{reviewControlsEnabled&&proposal.status==="pending"&&proposal.expiry_state==="active"?<DailyFarmBriefProposalReviewControls proposalRef={proposal.proposal_ref} expectedUpdatedAt={proposal.updated_at} initialStatus={proposal.status}/>:null}<p><Link href="/proposals/daily-brief">Daily Brief 確認事項一覧へ戻る</Link></p></article>;}
