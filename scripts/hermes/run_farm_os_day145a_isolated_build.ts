import{mkdtempSync}from"node:fs";import{tmpdir}from"node:os";import path from"node:path";import{spawnSync}from"node:child_process";const root=process.cwd(),dir=mkdtempSync(path.join(tmpdir(),"farmos-day145a-build-")),run=(c:string,a:string[],cwd=root)=>spawnSync(c,a,{cwd,encoding:"utf8",stdio:"inherit"});let r=run("rsync",["-a","--exclude=.git","--exclude=node_modules","--exclude=.next","--exclude=tsconfig.tsbuildinfo",`${root}/`,`${dir}/`]);if(r.status===0)r=run("rsync",["-a",`${root}/node_modules/`,`${dir}/node_modules/`]);if(r.status===0)r=run("pnpm",["run","build"],dir);console.log(JSON.stringify({schema_version:"farmos.day145a.isolated-build.v1",exit_code:r.status??1,production_execution_count:0,business_write_count:0,external_side_effect_count:0,repository_write_count:0}));process.exitCode=r.status??1;
