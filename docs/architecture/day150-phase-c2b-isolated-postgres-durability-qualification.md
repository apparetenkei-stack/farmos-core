# Day150 Phase C2-B2: image-authority source remediation candidate

## Classification and authority ceiling

This remediation changes source contracts only. B1 remains `QUALIFICATION_SOURCE_ARTIFACT_CREATED`; Image Authority is at most `V2_SOURCE_CANDIDATE`; B2 remains `NOT_AUTHORIZED`; Image remains `NOT_PINNED`; Qualification remains `NOT_EXECUTED`; Durability and Trusted clock remain `NOT_ESTABLISHED`; External probe remains `HOLD`; Gate 2 and Production remain `NOT_AUTHORIZED`; Runtime remains `NOT_BOUND`. It does not start Docker, inspect or pull an image, connect to PostgreSQL, apply or verify a migration, create canonical run evidence, adopt an image digest, or promote any execution state.

The exact C2-A lineage is commit `19889a78ae3a7d751c51f9b412f63c78bfc83a78`, migration `202608110001_production_target_execution_durability`, apply SHA-256 `e230647582fc3b1fb26d017034227cdf9b86384f6be7767f0c266ba4768ebc34`, and verify SHA-256 `ef8484ea130e930cd65e3b847869749727f00b2bf6ee373a2dc1d6d8fca0384f`. There is no latest, manifest-tail, fallback, or alternate-migration resolution.

## Image and isolation authority

`approved_repository_digest` and `observed_repository_digest` are distinct authorities. The approved digest is a human-approved B2 authorization input. The observed digest originates only from the positive Docker image-inspect projection; it is never copied from the authorization envelope, runtime reference, expected input, or tag selector. Future qualification requires exact byte equality between them.

The only canonical repository is the exact literal `docker.io/library/postgres`. Docker Engine may expose a locally inspected Docker Hub Official Image RepoDigest in the short representation `postgres@sha256:<64-lowercase-hex>`. The C2-B adapter therefore defines one narrow representation equivalence: under a verified C2-B inspect context only, exact `postgres@sha256:X` maps to exact `docker.io/library/postgres@sha256:X` with `X` preserved byte-for-byte. This is not generic Docker short-name normalization, does not establish remote registry provenance, and does not approve the resulting candidate.

Discovery canonicalization requires the exact selector `docker.io/library/postgres:17`; runtime verification requires the existing exact canonical digest reference. The context is derived from validated operation argv, and the mutable discovery selector is never admitted to the B2 runtime command allowlist. Exact short `X` and canonical `X` may byte-deduplicate as the one explicit C2-B equivalence pair. A different digest, any other repository identity, an unknown alias, malformed input, or missing/mismatched context fails closed or produces `HOLD_IMAGE_IDENTITY_AMBIGUOUS`. Equal content digests never collapse arbitrary repository identities. There is no first, last, latest, sorted, or approved-preference selection.

The positive image projection contains only `image_id`, `observed_repository_digest`, and exact structured `observed_platform` (`os`, `architecture`, and explicit `variant`, where `null` means inspect supplied no variant). Raw `RepoDigests`, `Config.Env`, `Config.Cmd`, `HostConfig`, `Mounts`, raw inspect data, and raw Docker errors do not cross the adapter boundary. Image ID identifies local content but is not distribution authority.

`expected_platform` is supplied by the B2 authorization authority and is separate from adapter-observed `observed_platform`. Future qualification requires exact OS, architecture, and variant equality; there is no host-derived default or silent cross-architecture/variant equivalence. Future B2 runtime authority is digest-only and always uses `--pull=never`. `docker.io/library/postgres:17` may be a `DISCOVERY_SELECTOR` only; it is never an approved digest or runtime authority. Canonicalization produces only a candidate representation; human image/digest/platform adoption remains separate. Pull is image acquisition, not representation normalization. Docker discovery, image acquisition, and image adoption remain `NOT_AUTHORIZED`.

The Day147 digest `sha256:5c855ad7b85e68e48a62f34662853f38b57c1c1d80f3a927ab58034fd6d31c5e` is retained only as `HISTORICAL_DAY147_QUALIFIED_IMAGE_IDENTITY`; it is not a fallback, default, or candidate for promotion.

Owned resources derive only from a caller-supplied 24-lowercase-hex execution nonce:

- container `farmos-pte-c2b-pg17-<nonce>`;
- label `farmos.day150.phase-c2b=<nonce>`;
- volume `farmos-pte-c2b-data-<nonce>`;
- internal network `farmos-pte-c2b-net-<nonce>`;
- test database `farmos_pte_c2b`;
- application name `farmos-day150-c2b-qualification`.

The container is non-privileged, has no host network, Docker socket mount, repository mount, credential-file mount, or production volume. It uses an internal bridge, localhost-only ephemeral port publication, bounded memory/CPU/pids, `--restart=no`, and an exact owned volume so state can survive an exact container restart inside one run. No wider binding or default-network fallback is permitted.

Docker command execution is argv-only with `shell:false`, a fixed local Docker socket, and an explicit environment allowlist. The allowlist contains exact image inspect, owned network/volume create/inspect/remove, exact container run/inspect/stop/start/kill/remove. It contains no pull, exec, prune, wildcard removal, or unowned deletion. Raw inspect JSON never crosses the Docker transport boundary: a positive projection retains only the exact image, container, network, volume, label, state, and localhost port fields required by qualification. `Config.Env`, full host configuration, mounts, unknown nested fields, and raw errors are discarded before any result is returned, followed by a defense-in-depth secret-pattern rejection.

Resource creation is tracked monotonically per container, volume, and network. Cleanup proves and removes each `CREATED_OWNED` resource independently in container, volume, network order. A missing container does not block an owned volume or network cleanup. `CREATED_UNOWNED_COLLISION` and `UNKNOWN` are never deleted and prevent qualification. Cleanup evidence records the exact final state and bounded identity of all three resources.

## Fixture and credential boundary

The fixture uses only `farmos_pte_c2b_owner`, `farmos_pte_c2b_runtime`, `farmos_pte_c2b_attacker`, database `farmos_pte_c2b`, and password grammar `c2b_<64-lowercase-hex>`. Credential material is injected from the isolated runner, never loaded from environment or a broker, and never enters evidence, a DSN log, Docker output, or raw error.

Synthetic records cover Proposal, Approval, Approval Receipt, Revocation Event and Head, Command, lifecycle, Reservation, Attempt, Receipt, Clock Evidence and Floor, Phase B snapshot, target binding, and reconciliation. They contain no production target, farm data, user data, provider identity, or production credential. C1 remains the semantic owner; the fixture only supplies deterministic C1-shaped inputs.

The isolated migration-history fixture reproduces the existing `core_schema.migration_history` column and constraint contract. Future B2 records only the exact C2-A ID, sequence, and apply checksum after exact apply bytes succeed, then executes the exact read-only verifier bytes. It never imports production database state.

## Executor and adapter separation

The executor owns orchestration, fixed migration and verifier plans, ordered case execution, fault/restart sequencing, evidence assembly, and cleanup invocation. It depends on an injected adapter and contains no Docker, PostgreSQL, environment, credential, G2-A, or production fallback.

Adapter behavior and real-execution authority are separate. The Docker adapter module owns a private WeakMap-bound capability that is issued only with its frozen real-adapter instance; public discriminator strings, booleans, object fields, and type assertions are not authority. A fake adapter may exercise the fixed source registry and return `SOURCE_VALIDATION_PASS`, but that path cannot emit QUALIFIED evidence, receipt, or commit marker. The B1 real boundary is deliberately fail-closed until a later authorized B2 implementation binds real operations inside the same authority-owning module.

The B2 authorization envelope authority is `farmos.production-target-execution-postgres-isolated-qualification-authorization.v1`. Its canonical digest binds the operation, nonce, exact C2-A identity, expected finalized C2-B source commit, exact approved image repository digest, `expected_platform`, case and fault registry identities, migration/apply/verify identities, expiry, and human-approval reference digest. The future expected source commit is the finalized remediation commit, not the historical B1 commit, and no unknown future SHA is hard-coded. The envelope defines the shape of future authorization but does not manufacture human approval. Qualification also requires an independently injected source-lineage resolver to return `PINNED_B1_COMMIT`, equal to the envelope. CLI source-commit strings, `true` flags, and `--real` switches are not accepted.

Concurrency uses independent PostgreSQL clients and transactions in one ordinary Node process. No process-local mutex is authority. Restart closes all pools, clients, and repository instances, restarts only the exact owned container while retaining the exact volume, and creates fresh instances before state readback. No custom IPC server, Unix socket listener, background daemon, or `tsx` CLI is required.

## Case and fault registries

The ordered case registry authority is `farmos.production-target-execution-postgres-qualification-case-registry.v1`. It has exactly 66 statically defined cases: MIG 8, GRD 5, SOT 9, RSV 4, REV 2, ATT 1, TERM 5, CLK 4, FLT-RSV 5, FLT-ATT 3, FLT-FIN 3, FLT-RCP 1, RST 9, CLN 4, and SAF 3. Each tuple binds case ID, category, expected-result profile, expected winner count, and bounded loser results. A PASS result must exact-match that profile and winner count; its authoritative mutation-row count must equal the fixed winner-count expectation, and every observed loser classification must be a unique member of the tuple allowlist. The domain-separated registry digest changes when any ordered authority field changes. Runtime callers cannot add cases.

The fixed fault registry distinguishes `APPLICATION_OBSERVATION_BOUNDARY_AND_CONTAINER_CRASH_BOUNDARY`. A committed-but-unobserved ACK case means the underlying COMMIT succeeds and the adapter suppresses success from the repository boundary; it is not represented as a wire-level proxy test. Deterministic pre-write faults use a fixture-admin lock and terminate only the exact observed fixture backend. They do not install schema-changing test triggers or accept an arbitrary backend ID.

## Evidence and failure preservation

The canonical historical evidence authority is v1 and is never parsed, upgraded, or reinterpreted as v2. HEAD before this remediation already contained the v2 identifier as an unexecuted B1 source placeholder, but B2 was not authorized, canonical evidence was never created, and that placeholder acquired no artifact authority. This remediation defines the first complete v2 candidate semantics under `V2_SOURCE_CANDIDATE`; it does not validate any predecessor shape as v2. The identifier is `farmos.production-target-execution-postgres-isolated-qualification-evidence.v2`. Exact v2 parsers reject missing and unknown keys. Evidence mode is exactly `ISOLATED_POSTGRES_QUALIFICATION` and separately binds `approved_repository`, `approved_repository_digest`, independently observed `observed_repository_digest`, `expected_platform`, `observed_platform`, `observed_image_id`, server versions, executor, authorization digest, case and fault registries, nonce, C2-A commit, expected and independently observed C2-B commits, migration and verifier identities, resource identity digests, all ordered case results, per-resource cleanup states, residual resources, production/external-network operations, retry count, bounded timestamps, fault model, and classification.

Classifications are `BLOCKED_ENVIRONMENT`, `FAILED_EXECUTION`, and `QUALIFIED`. Process exit alone cannot qualify. `QUALIFIED` requires approved/observed digest equality, expected/observed platform equality, a valid real-adapter capability, a valid B2 authorization and exact source lineage, all 66 exact PASS results, exactly three owned resources created and removed, successful cleanup, residual resources zero, production operations zero, external-network operations zero, and automatic retry zero. Digest or platform mismatch is `BLOCKED_PHASE_C2B_IMAGE_AUTHORITY`, never adoption or normalization. Receipt and commit-marker parsers also reject missing, additional, version-inconsistent, or digest-inconsistent fields.

The canonical path is `reports/day150-phase-c2b-isolated-postgres/runs/<nonce>/`. A completed run owns `evidence.json`; only exact QUALIFIED v2 evidence may produce a v2 receipt candidate; only that evidence plus its exact v2 receipt may produce a v2 commit-marker candidate. Receipt and marker explicitly bind the evidence/receipt authority versions, while their digests transitively bind observed digest and platform. A v1 receipt cannot validate v2 evidence. Changed evidence changes the receipt evidence digest; changed receipt changes the marker receipt and chain digests. Syntax parsers establish no semantic authority. The single high-level accepted-chain validator rejects orphan receipts, orphan markers, cross-version chains, and cross-evidence/platform chains. Files remain canonical JSON, exclusive-create, and digest-linked; a later run cannot overwrite or delete an earlier run. This remediation creates no canonical run directory or file.

## B2 hold and completion gate

B2 remains `NOT_AUTHORIZED` until the remediated source is separately staged, committed, pushed, and authorized; an exact finalized C2-B source commit, approved repository digest, and expected platform are approved; the exact image is local or acquisition is separately approved; Docker daemon and isolated-DB operations are explicitly approved; and all remaining gates pass. Current Image is `NOT_PINNED`; Docker discovery and image pull remain `NOT_AUTHORIZED`.

Technical QUALIFIED evidence requires exact migration apply/history/verifier/catalog/ACL/guards, Approval SOT durability, reservation/revocation/attempt/terminal/clock concurrency, every ACK-loss branch, restart/reopen/replay rejection, client closure, exact cleanup, residual resources zero, production access zero, P1 zero, P2 zero, and Sol GO. Successful evidence remains a candidate for a separate G2-A adoption decision. Trusted clock, external probe, Gate 2, runtime binding, production access, and production execution remain unauthorized.

## Historical review record

Historical findings remain immutable: Sol authorization failures `2`; prior B1 review `P1=3 / P2=3 / HOLD`; B1 remediation `P1=0 / P2=0 / GO`; image-authority preflight `P1=0 / P2=2 / REVISE`. This image-authority remediation is recorded separately and does not rewrite those results.

## R1 bootstrap manifest source authority

R1 defines the pure source contract `farmos.day150-c2b-bootstrap-manifest.v1` for purpose `DAY150_PHASE_C2B_ISOLATED_DURABILITY_QUALIFICATION`. Its canonical body binds the exact C2-B and C2-A source commits, adopted PostgreSQL repository digest and `linux/arm64/v8` platform, expected PostgreSQL major 17, case and fault registry authorities and digests, and the exact migration/apply/verify identities. It also binds the already human-adopted storage rollback, non-login semantic writer principal `farmos_c2b_bootstrap` and ledger-root/APFS policy, actor capability generations and terminal challenge states, privileged-writer/UI split, immutable publication and orphan quarantine/recovery, global-generation CAS, clock recovery and epoch supersession, 900-second authorization-start TTL, 30-second attempt-to-spawn deadline, process-local execution-fence consumption/crash outcomes, conditional hash-integrity sufficiency, and privacy policies. The writer profile fixes executable mode `0555`, a root-owned non-writable parent chain, cwd `/`, umask `0077`, and an empty or exact minimal manifest-bound environment. This is a policy representation; it does not re-adopt or redesign those policies.

The manifest body is serialized using the repository-established canonical contract JSON algorithm with recursively sorted keys and UTF-8 bytes. Its digest is domain-separated SHA-256 under `farmos.day150-c2b-bootstrap-manifest.v1:manifest-body`; the digest field is outside the digest preimage. Exact parsers accept only dense ordinary arrays and data-only plain objects, and reject missing or unknown keys, symbols, accessors, exotic prototypes, sparse or named-property arrays, reflective failures, incorrect fixed identities or policy values, malformed digests, and any claim that an unresolved implementation profile or runtime state is established. Accepted input is projected to the fixed source candidate before return, so caller object behavior cannot become authority. There is no latest-version resolution, fallback, auto-upgrade, normalization, signature, or keyed integrity mechanism in R1.

The Day150 closure candidate resolves the implementation profile as deterministic future-install rules rather than invented host facts. Artifact digests, dynamic UID/GID, APFS device identity, launch digest, and adoption evidence are assigned only by the separately authorized installer after collision checks and independent readback. The source candidate still records false runtime claims. It is only `BOOTSTRAP_MANIFEST_SOURCE_CONTRACT`, not an adopted manifest instance, Gen0 record, runtime attestation, active bootstrap authority, durable approval, trusted clock, or B2 authorization.

R1 implements no actor authentication, writer protocol, ledger, filesystem operation, clock observation, nonce, reservation, `ATTEMPT_STARTED`, execution fence, Docker operation, native helper, installation, or Gen0 ceremony. Later publication durability cannot satisfy the adopted dirfd-relative, atomic no-replace, and Darwin full-durability requirements with the current Node API surface alone; a small native helper remains future, separately authorized work. R1's maximum state is `SOURCE_CONTRACT_CREATED`. B2 remains `NOT_AUTHORIZED`, B2 execution remains `NOT_EXECUTED`, Gate 2 remains `NOT_AUTHORIZED`, Runtime remains `NOT_BOUND`, and Production remains `NOT_AUTHORIZED`.

## R2 bootstrap ledger source semantics

R2 defines only a structurally valid source-chain candidate under record authority `farmos.day150-c2b-bootstrap-ledger-record.v1` and digest domain `farmos.day150-c2b-bootstrap-ledger-record.v1:record-body`. Structural validity means only that the supplied data is schema-valid, digest-valid, generation-valid, predecessor-valid, transition-valid, and policy-valid. It does not mean that any record is published, durable, current, trusted, authoritative, canonical, or observed from the adopted ledger root. R2 has no trusted-publication provenance type, storage adapter, filesystem path, writer, or runtime authority. Its successful replay and status vocabulary is limited to `STRUCTURALLY_VALID_SOURCE_CHAIN_CANDIDATE`, `SOURCE_CHAIN_REPLAY_CANDIDATE`, and `SOURCE_PROJECTION_ONLY`.

The record body contains only its fixed schema, authority and revision, generation, previous generation and digest, the R1 bootstrap manifest digest, one typed event, and a bounded projected-state claim. The only R2 event kinds are `BOOTSTRAP_GENESIS` and `QUARANTINE_ENTERED`. The source Gen0 candidate has generation zero, null predecessors, the fixed R1 manifest digest, and bootstrap authority `NOT_ACTIVE`; it is not a runtime Gen0. `QUARANTINE_ENTERED` carries one finite policy reason, is terminal and non-repeatable, has no R2 recovery transition, and derives only `QUARANTINE_REQUIRED_IF_TRUSTEDLY_OBSERVED`. That state is conditional policy: only a future storage-owned trusted readback could establish the triggering observation and cause a later runtime authority to quarantine.

Replay snapshots caller input once into dense ordinary arrays and data-only plain objects, rejecting symbols, accessors, exotic prototypes, sparse or named-property arrays, and reflective failures. It never sorts, repairs, deduplicates, selects a highest generation, or returns partial candidate state after failure. Validation precedence is fixed: replay shape; envelope shape; body shape; record identity; generation and predecessor grammar; R1 manifest binding; event kind and payload; projection shape; digest grammar and equality; duplicate or divergent-generation collision; Gen0 requirements; generation continuity; predecessor generation and digest; transition terminality; derived projection equality; then final source-candidate classification. Exact duplicates and divergent same-generation records are both rejected.

R2 CAS results are source simulations only: `SOURCE_ACCEPTABLE_SUCCESSOR_CANDIDATE`, `SOURCE_CAS_CONFLICT`, `SOURCE_EXACT_MATCH_REQUIRES_TRUSTED_READBACK`, `SOURCE_INVALID_TRANSITION`, or `QUARANTINE_IF_TRUSTEDLY_OBSERVED`. An exact structural match is never runtime idempotence. Advisory orphan observations expose only source-level match, absent, or unknown relationships; publication, durable membership, and confirmed presence or absence require a later storage-owned trusted readback. Sanitized status is a disjoint union: the valid branch contains fixed authority metadata and fully validated candidate generation, candidate digest, and bounded source projection; the invalid branch contains only fixed authority metadata and a finite failure reason.

R2 performs no filesystem write, ledger publication, native operation, Docker or PostgreSQL operation, migration, actor or clock establishment, authorization issuance, B2 execution, runtime binding, or production operation. Its maximum state is `SOURCE_AUTHORITY_CANDIDATE`: the ledger record contract, generation reducer, and sanitized status projector exist as source, while the structural chain remains `CANDIDATE_ONLY`; runtime ledger and runtime Gen0 remain `NOT_CREATED`; bootstrap authority remains `NOT_ACTIVE`; trusted storage provenance, actor, and trusted clock remain `NOT_ESTABLISHED`; B2 authorization remains `NOT_ISSUED`; B2 remains `NOT_EXECUTED`; Gate 2 remains `NOT_AUTHORIZED`; Runtime remains `NOT_BOUND`; Production remains `NOT_AUTHORIZED`.

## R3 actor and trusted-clock source intents

R3 uses Model B: actor authorization intent and clock transition intent are separate immutable source-candidate contracts. They are not R2 ledger records, and R2 remains import-only with exactly `BOOTSTRAP_GENESIS` and `QUARANTINE_ENTERED`. An expected R2 generation and head digest is only a proposed source-base binding; it is not a current, published, durable, or storage-observed head. Candidate or digest equality is not replay protection, challenge consumption, runtime idempotence, or trusted readback.

The actor contract authority is `farmos.day150-c2b-bootstrap-actor-intent-source.v1`, with digest domain `farmos.day150-c2b-bootstrap-actor-intent-source.v1:candidate-body`. Its maximum success classification is `STRUCTURALLY_VALID_ACTOR_INTENT_SOURCE_CANDIDATE`. It binds only the fixed R1 manifest, expected R2 source base, purpose, one finite scope, domain-separated actor and challenge reference digest candidates, authentication-mechanism revision, proposed capability generation and lineage, and a bounded proposed validity window. The actor-reference domain is `farmos.day150-c2b-bootstrap-actor-reference.v1:generateduid-install-binding`, but R3 does not hash or accept a raw GeneratedUID. Authoritative reference generation requires a future native-owned OpenDirectory lookup and installation binding. R3 never proves challenge unpredictability, issuance, freshness, non-consumption, single use, interactive authentication, capability issuance, activation, replacement, or revocation.

The clock contract authority is `farmos.day150-c2b-bootstrap-clock-intent-source.v1`, with digest domain `farmos.day150-c2b-bootstrap-clock-intent-source.v1:candidate-body`. Its closed source intent kinds are `CLOCK_GENESIS_INTENT`, `CLOCK_COMPARISON_INTENT`, and `CLOCK_EPOCH_SUPERSESSION_INTENT`. Genesis binds only proposed installation, actor, capability, epoch, and timestamp values. Comparison returns only `NON_REGRESSING_IF_TRUSTEDLY_OBSERVED`, `ROLLBACK_CONDITION_IF_TRUSTEDLY_OBSERVED`, or `FORWARD_POISON_CONDITION_IF_TRUSTEDLY_OBSERVED`, nested under `SOURCE_RELATIVE_CANDIDATE_COMPARISON_ONLY`. All comparison inputs remain untrusted: a future decision requires authoritative epoch, durable floor, bound policy, OS observation, and storage readback together. The explicit upper-bound timestamp is candidate policy input and does not establish trusted time. Supersession binds the previous and proposed epochs, finite poison reason, suspect interval, proposed corrected genesis, recovery actor/capability references, and quarantine-pending affected-record policy. It neither activates an epoch nor clears quarantine.

Both parsers snapshot hostile JavaScript input once, accept only exact-key data-only ordinary objects and dense arrays, reject symbols, accessors, exotic prototypes, sparse or named arrays, and fail closed on reflection errors. They reuse the repository canonical JSON and domain-separated SHA-256 utilities and return sanitized disjoint unions. Invalid results contain only fixed authority metadata and one finite reason. No raw GeneratedUID, username, authentication material, native output, timestamp fragment, actor reference, capability, epoch, storage evidence, raw error, environment, argv, path, credential, or connection data is returned on failure.

Exported canonicalization and digest functions are source-construction utilities only. They do not validate hostile input, authenticate an actor, issue or activate a capability, prove challenge replay protection, establish clock provenance, publish a record, or confer authority. A self-consistent digest over an invalid or caller-extended body remains invalid until the exact parser accepts the reconstructed bounded candidate.

R3 performs no interactive authentication, challenge or capability issuance, OS clock observation, durable floor update, epoch activation, filesystem publication, native operation, ledger write, authorization, Docker or PostgreSQL operation, migration, B2 execution, runtime binding, or production operation. Its maximum state is `SOURCE_AUTHORITY_CANDIDATE`: both source contracts are `CREATED`; Actor and Trusted clock remain `NOT_ESTABLISHED`; Interactive authentication remains `NOT_VERIFIED`; Challenge and Capability remain `NOT_ISSUED`; Clock epoch remains `NOT_ACTIVE`; Durable monotonic floor remains `NOT_ESTABLISHED`; runtime ledger and Gen0 remain `NOT_CREATED`; bootstrap authority remains `NOT_ACTIVE`; B2 authorization remains `NOT_ISSUED`; B2 remains `NOT_EXECUTED`; Gate 2 and Production remain `NOT_AUTHORIZED`; Runtime remains `NOT_BOUND`.

## R4-1 pure runtime-provenance source contracts

R4 adopts Model R4-A without activating it. The new record authority `farmos.day150-c2b-bootstrap-runtime-provenance-record.v1` and digest domain `farmos.day150-c2b-bootstrap-runtime-provenance-record.v1:record-body` describe the only future runtime serialization domain. Every structural record uses one `generation`, `previous_generation`, and `previous_record_digest`; actor, challenge, capability, clock, quarantine, and recovery candidates do not receive independent authoritative heads or counters. R1, R2, and R3 remain immutable import-only source authorities. In particular, R2 continues to contain exactly `BOOTSTRAP_GENESIS` and `QUARANTINE_ENTERED`; R4 events do not extend or reinterpret R2.

R4-1 validates only `STRUCTURALLY_VALID_RUNTIME_PROVENANCE_RECORD_CANDIDATE` and `STRUCTURALLY_VALID_RUNTIME_PROVENANCE_CHAIN_CANDIDATE`. Its reducer is deterministic, does not sort, repair, deduplicate, select a highest generation, retry, observe storage, or resolve an ambiguous publication. Its projection is always marked `SOURCE_PROJECTION_ONLY`. Source equality is expressly not runtime idempotence, and a Git object, valid digest, parser success, or caller status never proves publication, durability, canonical membership, current head, actor authentication, challenge/capability lifecycle, trusted time, epoch activation, durable floor, rollback detection, or runtime Gen0. Future canonical membership begins only at the storage-owned atomic no-replace publication point; a result is not safe to acknowledge as committed until file and directory durability, final dirfd-relative reopen/readback, and full-chain replay have all succeeded.

The closed R4-1 event vocabulary covers only integrated Gen0, challenge issuance and terminalization, one-shot capability issuance and terminalization, clock-floor advancement, rollback quarantine, append-only epoch supersession, runtime quarantine entry, and recovery recording. It excludes B2 authorization, `ATTEMPT_STARTED`, execution-fence consumption, Docker spawn, Gate 2, and production operations. `OUTCOME_UNKNOWN_CANDIDATE` is terminal source vocabulary, not a resolution: automatic retry and cleanup remain zero, and only a future trusted storage reopen/readback may recognize an exact already-published record under a separately implemented runtime result.

The integrated Gen0 structure binds distinct proposal, human approval decision, and approval receipt references; R1/R2/R3 lineage; installation and native profiles; actor reference; native ceremony session; initial OS-CSPRNG challenge and terminal consumption; terminal one-shot bootstrap capability; OS UTC observation; human time-plausibility confirmation; epoch zero; initial floor; and interpretation-policy revisions. Field presence does not infer human approval. The cycle-free bootstrap order remains installation/native profile verification, native ceremony-session freshness, OS-backed challenge, fresh interactive macOS authentication, transient OpenDirectory GeneratedUID resolution and domain-separated actor-reference derivation, proposal and OS UTC presentation, explicit human approval, integrated Gen0 candidate construction, then future durable publication/readback. Initial ceremony freshness is native/session provenance and does not depend on a FarmOS trusted clock. Actor and trusted-clock facts may become established together only after a later trusted Gen0 publication and readback.

The Gen0 proposal reference is recomputed from a canonical versioned proposal body that binds the exact source/profile target digest; the canonical `APPROVE` decision body explicitly links that proposal, actor, initial challenge, and authentication-mechanism revision; and the canonical receipt body explicitly links the proposal, decision, actor, terminal challenge, and terminal bootstrap capability. All three use separate domain-separated digests and their references must be distinct. After Gen0, challenge and capability references cannot be reissued anywhere in the candidate chain, capability generations advance exactly by one from the current capability-or-revocation lineage head, and actor/session/freshness lineage must remain consistent. Trusted-clock issuance time must strictly advance the floor, expiry must follow issuance, and terminal observation must not exceed the projected expiry. Every ordinary actor-authority transition co-binds the active epoch, prior and proposed floor, OS observation, continuous-time bracket, and boot-session reference, advancing the floor in the same global candidate generation. Poisoned-clock recovery is the sole exception: it uses a separately identified native recovery session, can issue only recovery-lineage challenge/capability candidates while quarantined, and epoch supersession requires and terminally consumes that active one-shot recovery capability. Successful supersession retains the old-epoch affected-record policy by reference while clearing the global quarantine for the new epoch; a missing recovery capability leaves the chain held. Any `OUTCOME_UNKNOWN_CANDIDATE` blocks all further source transitions; pure source semantics cannot resolve it.

`DAY150_C2B_CROSS_EPOCH_CHALLENGE_RECOVERY_AMENDMENT_V1` additively permits one narrowly bound `CHALLENGE_TERMINALIZATION_CANDIDATE` when trusted replay proves an old-epoch `OUTSTANDING_CANDIDATE`, an independently valid native observation proves a different boot session, and an exact one-shot FarmOS-clock-independent recovery session binds the current generation/head, old epoch, old challenge/session, both boot references, and amendment digest. Its sole terminal state is `BOOT_SESSION_INVALIDATED_CANDIDATE`; it records no authoritative UTC terminal time, does not advance the floor, issue capability, authorize execution, or reinterpret historical records. The resulting projection blocks ordinary challenge issuance and floor advancement until the frozen recovery-capability and `CLOCK_EPOCH_SUPERSESSION_CANDIDATE` flow becomes valid. The amendment does not make epoch supersession implicitly invalidate challenges and does not alter the v1 record authority or previously valid terminalization payloads.

The disposable reboot qualification reached that terminal state at generation 2 and then stopped fail-closed: the frozen recovery challenge requires rollback-quarantine state, while the cross-epoch terminalization deliberately preserves the old clock projection as established/non-quarantined and cannot assert rollback under the discontinuous boot session. No recovery challenge, recovery capability, epoch supersession, or authentication attempt follows until a separately adopted narrow recovery-capability transition connects this state to the existing supersession prerequisites.

`DAY150_C2B_BOOT_SESSION_RECOVERY_CAPABILITY_AMENDMENT_V1` supplies that narrow eligibility branch without adding an event kind or altering rollback recovery. An existing recovery challenge may follow only the current, fully replayed `BOOT_SESSION_INVALIDATED_CANDIDATE` head and must carry the exact Gen2 terminal lineage, historical challenge/session, old epoch and boot, independently observed current boot, one-shot recovery session/freshness, exact predecessor head, policy revision, and sole purpose `CLOCK_EPOCH_SUPERSESSION_CANDIDATE`. The same immutable lineage is carried through existing challenge terminalization and recovery capability issuance events. The capability has no ordinary challenge, ordinary capability, B2, filesystem, database, or generic recovery authority; the immediately following existing epoch-supersession transition consumes it. The rollback/quarantine branch remains unchanged and boot discontinuity is never represented as rollback.

The preserved disposable chain applied this branch append-only at generations 3–6, activated a new epoch bound to the current boot session, and then issued a wholly new ordinary challenge at generation 7. Exactly one signed-companion authentication succeeded and generation 8 terminally consumed that challenge. Product Owner authority `DAY150_POST_GEN0_NORMAL_CAPABILITY_TTL_POLICY_V1` adopts an exact 120-second maximum lifetime for only the normal one-shot capability issued from that accepted post-Gen0 authentication lineage. Its issuance and consumption require separate `NativeTrustedClockPolicy`-accepted observations and exact Actor, challenge/session, purpose/scope, epoch, boot, durable-floor, generation, and global-head bindings. Expiration never renews or slides, and epoch/boot/head mismatch, rollback, quarantine, `OUTCOME_UNKNOWN`, revocation, or provenance failure invalidates the capability earlier. The 300-second ceremony, 180-second authentication evaluation, 120-second normal capability, 900-second authorization-start, and 30-second attempt-to-spawn timing domains remain independent.

After complete Gen0→Gen8 trusted replay and a 21-case injected trusted-observation matrix, generation 9 durably issued exactly one normal qualification capability and generation 10 terminally consumed it through the existing `CAPABILITY_ISSUANCE_CANDIDATE` and `CAPABILITY_TERMINALIZATION_CANDIDATE` vocabulary. Fresh-process replay reconstructed one 11-record global chain with head `sha256:d6a0dd63fc8e1d8f3d24d31814a0d99628250170083e28854384d9794b96cc90`; the active epoch and boot session remained unchanged, Gen0/Gen1/Gen2 bytes remained unchanged, and no B2, Docker, canonical Gen0, canonical activation, or production authority was created.

The source-only native protocol authority `farmos.day150-c2b-bootstrap-native-protocol.v1` has a finite operation vocabulary for candidate ceremony, actor and clock validation, candidate record publication, and sanitized status read. It accepts no arbitrary command, argv, environment, path, ledger root, filesystem operation, authentication operation, Keychain request, Docker, database, or network authority. Parsed messages prove neither binary/code-signing/peer identity nor publication. Installation and native profile fields are bounded digest references only; no installed artifact is created or validated in R4-1.

Future native work preserves the reviewed split. A root-owned validator/broker validates peer, request, authentication, and identity but never writes the ledger. A per-mutation worker receives one bounded canonical request over a private channel, closes unrelated file descriptors, removes supplementary groups, performs `setgid` then `setuid` to the dedicated writer with no root regain, uses cwd `/` and umask `0077`, prohibits network and exec, and accesses only the canonical ledger. The canonical root `/Library/Application Support/FarmOS/day150-c2b-bootstrap/v1` is dedicated-writer-owned mode `0700`; records and temporary files are writer-owned regular mode `0600` with link count one; controlled parents are root-owned, non-writable, and traversal-only. ACL or hidden grants require separate adoption.

Future storage publication remains dirfd-relative on qualified local APFS: verify the entire chain and expected head, create a same-device exclusive temporary record, write exact bounded bytes, perform required full file durability, publish atomically with no replacement, durably sync the parent directory, reopen/read back, and recompute the full state before ACK. Crashes or connection loss after possible namespace publication remain `OUTCOME_UNKNOWN`; there is no automatic retry or cleanup. Future storage-owned CAS has one winner, requires genesis-to-head verification for every mutation, discards losing candidates, performs full trusted readback, and never converts source equality into a runtime commit result.

The sanitized status source contract exposes only bounded conditional enums, candidate generation/head digests, and a provenance reference. It cannot manufacture runtime facts and excludes raw GeneratedUID, username, challenge or capability material, ledger bytes, paths, native output, credentials, tokens, raw errors, stderr, stacks, environment, argv, DSNs, and connection strings. The clock structure may reference OS UTC observations, continuous-time brackets, boot-session digests, epochs, floors, and policy revisions, but R4-1 performs no clock read. The future execution fence remains same-process, one-shot, non-serializable, non-reconstructable after restart, and outside ledger/protocol/status surfaces.

R4-1's maximum state is `SOURCE_AUTHORITY_CANDIDATE`: the runtime-provenance record contract, pure reducer, bounded native-protocol source contract, and sanitized-status source contract are `CREATED`; the runtime chain is `CANDIDATE_ONLY`. Runtime ledger, canonical root, writer principal, native helper, and runtime Gen0 remain `NOT_CREATED` or `NOT_BUILT`/`NOT_INSTALLED`; Actor and Trusted clock remain `NOT_ESTABLISHED`; Challenge and Capability remain `NOT_ISSUED`; Clock epoch remains `NOT_ACTIVE`; Durable floor remains `NOT_ESTABLISHED`; bootstrap authority remains `NOT_ACTIVE`; B2 authorization remains `NOT_ISSUED`; B2 remains `NOT_EXECUTED`; Gate 2 and Production remain `NOT_AUTHORIZED`; Runtime remains `NOT_BOUND`. R4-2 native build qualification, R4-3 disposable APFS storage qualification, R4-4 installation profile, R4-5 actor provenance, R4-6 clock provenance, R4-7 integrated rehearsal, R4-8 canonical installation, and R4-9 live Gen0 ceremony all require separate human-authorized gates.

## R4-2 native package source and local build qualification

R4-2 adds one dependency-free repository-local Swift package under `native/day150-c2b-bootstrap`. Its core library mirrors the finite `farmos.day150-c2b-bootstrap-native-protocol.v1` request and response schema and contains separate source abstractions for broker validation, writer privilege-drop policy, Darwin storage primitives, actor-reference derivation, and clock-observation fixtures. Three executable targets represent the future unprivileged ceremony/status client, root-owned validator/broker, and dedicated-principal writer worker. Their default binaries only emit bounded inactive source-target status; building or invoking them does not install, privilege, authenticate, publish, or activate any runtime authority.

The broker source accepts only private-channel peer candidates and the three broker validation/observation operations. It has no ledger-write, shell, Docker, database, or network authority. The writer source fixes the reviewed order: close unrelated descriptors, remove supplementary groups, `setgid`, `setuid`, reject root regain, set cwd `/`, apply umask `0077`, deny network and exec, then accept one bounded writer operation. R4-2 models and tests this ordering but performs no privilege change. Actor fixtures require an accepted interactive native-boundary result and transient GeneratedUID input to derive a domain-separated digest; raw GeneratedUID, username, and authentication blobs are never retained. Clock tests use injected observations only, and neither fixtures nor a caller timestamp establish trusted time.

Darwin storage source identifies `openat`-style exclusive/no-follow flags, owner/mode/device checks, regular-file link-count checks, `F_FULLFSYNC`, `renameatx_np` `RENAME_EXCL`, parent-directory durability, and final dirfd-relative readback as the future R4-3 primitive plan. R4-2 validates argument and failure classifications only. It never touches `/Library/Application Support/FarmOS/day150-c2b-bootstrap/v1`, creates no writer principal or ledger root, and publishes no record. The dependency-free test harness is intentionally the `FarmOSDay150C2BNativeQualification` executable target because the selected offline SDK supplies neither Swift Testing nor XCTest; qualification requires executing that exact product and observing its nonzero-on-failure result, so `swift test` alone is not evidence. Every SwiftPM command must use an explicit repository-external scratch and cache path, and finalization must reject any package-local `.build` or `.swiftpm` path. Local SwiftPM outputs and their hashes are qualification evidence only, never installed-binary identity. Code signing, launchd profiles, installation, disposable APFS publication qualification, canonical storage, actor establishment, clock establishment, runtime Gen0, authorization, and B2 execution remain separately gated.

R4-2's maximum state is `SOURCE_COMPLETE` with native build `QUALIFIED_LOCAL_ONLY`. Native installation remains `NO`; Writer principal, canonical root, runtime ledger, and runtime Gen0 remain `NOT_CREATED`; Actor and Trusted clock remain `NOT_ESTABLISHED`; Challenge and Capability remain `NOT_ISSUED`; bootstrap authority remains `NOT_ACTIVE`; B2 authorization remains `NOT_ISSUED`; B2 remains `NOT_EXECUTED`; Gate 2 and Production remain `NOT_AUTHORIZED`.

## Disposable local-APFS storage qualification

The Day150 disposable-storage gate qualifies the native storage model only under the repository-external, current-user-owned root `/Users/hayate/Library/Application Support/FarmOS/day150-c2b-bootstrap-qualification/r4-storage`. This root is qualification evidence, not the canonical ledger root, an installed runtime, or dedicated-writer identity. The canonical root `/Library/Application Support/FarmOS/day150-c2b-bootstrap/v1` remains untouched. Each mutation is confined to a newly created mode-`0700` run directory on local APFS; record and temporary files are mode `0600`, regular, link-count one, current qualification UID/GID-owned, and on the trusted root device.

The real qualification writer retains a no-follow root directory descriptor and performs strict-name record operations relative to it. The controlled ancestor chain is checked for ordinary directories, expected root/current-user ownership, no group/other write, one APFS device, and root path/opened-fd inode identity. Before mutation it enumerates and replays the complete contiguous chain, independently validates the exact R4-1 bindings, nested proposal/approval/receipt lineage, candidate projection, canonical bytes, and digest, then binds the expected generation and head. Publication uses exclusive same-directory temporary creation, bounded exact writes, file `F_FULLFSYNC`, `renameatx_np` with `RENAME_EXCL`, directory durability, final no-follow reopen/readback, and full-chain replay. Namespace publication is canonical membership inside a disposable run only; `QUALIFICATION_COMMITTED` is not returned until directory durability and final replay succeed. A CAS loser performs no retry and cannot publish a later generation.

The directory-durability probe ran only against a new disposable APFS run descriptor. Both `fsync(directory_fd)` and `fcntl(directory_fd, F_FULLFSYNC)` returned success on the qualified platform, and the gate selects `F_FULLFSYNC_DIRECTORY`; rename alone is never sufficient. This evidence establishes bounded syscall acceptance and ordering on the qualified local APFS environment; it does not claim an unperformed reboot or physical power-loss experiment. The probe records only its operation identifiers, bounded status, filesystem type, and qualification toolchain context. It neither establishes a canonical runtime primitive nor weakens the adopted durability policy on unsupported systems.

An earlier post-publication caller result remains permanently `QUALIFICATION_OUTCOME_UNKNOWN`. Read-only verification of preserved `run-0000000000000001` proved its exact mode, owner, link count, device, file SHA-256, record digest, generation-zero predecessor values, parser acceptance, and full one-record replay, yielding the separate classification `QUALIFICATION_ALREADY_PRESENT_AFTER_TRUSTED_READBACK`. The historical result is not rewritten. A second preserved post-publication artifact, `run-0000000000000003`, and later fixed fault-point evidence were reconciled under the same bounded readback procedure. Historical source-compatible fixtures remain read-only evidence and are not accepted for new publication. Preserved runs are accepted only through explicit qualification-harness evidence parameters, remain read-only, cannot receive a later generation, and are never hard-coded into native runtime authority.

The resumed crash matrix creates only new run directories. Every storage attempt first publishes and fully syncs a generation-specific bounded `mutation-pending-<generation>.json` fence containing the exact candidate record digest before creating its record temporary. The fence is append-only qualification evidence: it is never unlinked, even after final record durability and exact replay. Runtime replay enforces an exact bidirectional one-to-one mapping between every contiguous `record-N` and `mutation-pending-N`, including metadata and digest equality; zero records is valid only with zero fences. Removing every fence or one middle-generation fence leaves the run `OUTCOME_UNKNOWN` and cannot enable fresh-process N+1. A successful caller keeps the exact fence in a process-local reconciled set only after durability/readback checks; a fresh process remains fail-closed until an explicit trusted readback validates every fence, corresponding record, and the complete chain. This removes any unlink-then-directory-sync or best-effort fence-restoration ambiguity. Consequently a child crash at any fixed boundary leaves durable run-local ambiguity that a fresh process cannot turn into N+1. Pre-publication orphan evidence and post-publication ACK-ambiguity evidence are preserved; an unknown run blocks mutation, retry, cleanup, and next-generation publication. Explicit trusted readback may recognize the exact published record without clearing the pending fence or changing the original caller outcome. The harness can re-open legacy record-only or `mutation-pending.json` evidence and generation-specific `{pending}`, `{pending,temp}`, `{pending,record}`, complete/partial/missing-fence chains, and bounded completed test fixtures only through exact sorted entry/SHA manifests; this harness-only compatibility seam is not runtime replay authority. Missing, extra, corrupt, symlink, hard-link, wrong-mode, truncated, or readback-mismatched state fails closed. Collision/CAS-loser recognition is read-only and replays the entire fenced chain; there is no secondary public no-replace mutation path.

This qualification establishes only `QUALIFIED_DISPOSABLE_ONLY` for publication, CAS/concurrency, and crash semantics. Native installation remains `NO`; Writer principal, canonical ledger root, canonical runtime ledger, and runtime Gen0 remain `NOT_CREATED`; Actor and Trusted clock remain `NOT_ESTABLISHED`; Challenge and Capability remain `NOT_ISSUED`; bootstrap authority remains `NOT_ACTIVE`; B2 authorization remains `NOT_ISSUED`; B2 remains `NOT_EXECUTED`; Gate 2 and Production remain `NOT_AUTHORIZED`.
