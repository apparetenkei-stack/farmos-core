# Day150 Prefix Expected Catalog Reference Derivation

Status: `V5_CONSUMED_EXACTLY_ONCE / V5_EXTERNAL_RESOURCES_COMPENSATED / V6_CONSUMED_TERMINAL / V6_ATTEMPT_BINDING_DEFECT_HISTORICAL / V6_RETRY_FORBIDDEN / V6_EXTERNAL_RESOURCES_COMPENSATED_ZERO_RESIDUAL / V7_RETIRED_NON_RUNNABLE / V8_AUTHORIZED_BUT_NOT_CONSUMED_INVOCATION_EXHAUSTED_RETRY_FORBIDDEN / V9_CONSUMED_TERMINAL_POSTGRES_STARTUP_FAILED / V9_RETRY_FORBIDDEN / V9_EXTERNAL_RESOURCES_COMPENSATED_ZERO_RESIDUAL / V11_CONSUMED_OUTCOME_UNKNOWN_RETRY_FORBIDDEN / V11_DATABASE_EXACTLY_AFTER_MIGRATION_1 / V11_EXTERNAL_RESOURCES_COMPENSATED_ZERO_RESIDUAL / V12_INVOCATION_ISSUED_HUMAN_ALLOWANCE_EXHAUSTED_DURABLE_CONSUMPTION_NOT_REACHED_RETRY_FORBIDDEN / V13_APPROVED_EXECUTED_EXACTLY_ONCE_SUCCESSFUL_NON_RUNNABLE / EXACT_FIVE_CANDIDATES_PROMOTED_APPROVED / SUCCESS_VALID_PRESENT / TERMINAL_ABSENT / ZERO_RESIDUAL_TRUE / GATE17_PASS / EXACT_PREFIX_EXPECTED_CATALOG_AUTHORITY_APPROVED`.

## Current V13 exact-five promotion state

V13 is `APPROVED / EXECUTED EXACTLY ONCE / SUCCESSFUL / NON_RUNNABLE`. Its exact five
expected-catalog candidates were promoted atomically under the existing repository-owned approved
authority contract and are now `APPROVED`; their original unapproved bytes and identities remain
immutable historical lineage. The V13 SUCCESS receipt is `VALID / PRESENT`, the V13 TERMINAL receipt is
`ABSENT`, and exact owned-resource readback is `ZERO_RESIDUAL: TRUE`. Production, canonical, B2,
and Gate2 operations remain zero. Gate17 is `PASS` and the exact prefix expected-catalog authority
is `APPROVED`.

The Product Owner promotion was approved at `2026-08-20T06:43:00.000Z`. The existing promotion
contract derived approval reference
`product-owner/day150/exact-five/5b0424f24a16fd21931d2e6dfe83a69524755e04449e3d0f43f5b7c3cb729d1a`
and approval digest
`sha256:22a7c11942868a58d2fa5e58bc17cf7a0cd6a6f0e1857f6b5427f2eed071dc48`.
It binds candidate-set digest
`sha256:658b3765e28dd8050da393a167f812364887887bbe40fac7860206d9ccecaab2`,
corrected SOURCE qualification evidence v2 digest
`sha256:f34b2609279cfb801e732c535181a05828815dcfbd40ba89292b31f41dbdf382`,
and V13 SUCCESS receipt digest
`sha256:11f9730704ec3dcd3fd1014c8e2b9ddbf292c910c5f3e301826f3bdf29613826`.

The immutable qualification v1 artifact is historical rejected evidence classified
`SOURCE_QUALIFICATION_EVIDENCE_V1_REJECTED_FINGERPRINT_SEMANTICS`. Its generic catalog-object
fingerprints were generated by `createFarmOsMigrationObjectFingerprint`; they are not V13 candidate
fingerprint values and must not be reinterpreted as such. Qualification evidence v2 is a separate,
non-replacing `SOURCE_QUALIFICATION_EVIDENCE` revision generated from the isolated qualification
database state. It uses the same canonical
`createFarmOsDay150DualPrincipalSemanticFingerprint` authority as the REAL candidate contract,
semantic owner `REFERENCE_MIGRATION_OWNER`, semantic executor `REFERENCE_MIGRATION_EXECUTOR`, and
principal-normalization authority `farmos.reference-migration-owner-executor-normalization.v2`.
The authoritative comparison version is
`farmos.pg-catalog-semantic-principal-fingerprint.v3`. After durable v2 readback, all five corrected
SOURCE rows matched the successful V13 REAL candidates exactly across migration identity, migration
SHA, dual-principal semantic fingerprint, normalized snapshot digest, object count,
object-universe digest, catalog-query digest, initial-catalog authority, and ordered stage.

Historical V13 executable source remains
`sha256:b8a95697a2439a31d180706878ceb1c66171ba563e82037bf18518f382bccfa6`
and is distinct from post-V13 qualification tooling source. The repair did not retry V13, rewrite
candidate files or create new execution authority. Subsequent promotion materialized only the
reviewed exact-five approved authority; it did not regenerate any candidate.
The v2 artifact binds the exact qualification tooling source used for its one executed run as
`sha256:1b470dde147a4ff3e260dbafd38a60018846c203bb758527122a814843030849`.
After publication, the source-only entrypoint was fail-closed hardened so durable v2 presence stops
replay before qualification resource creation; this does not rewrite the executed-run source
identity or the immutable v2 artifact.

## V12 verified-runtime closure mismatch and Human invocation exhaustion reconciliation

V12 remains an exact historical record. Its Product Owner approval record and digest
`sha256:1745f4892c2846a6753ef36c94b404be88fc7e596d4b88e7cc7df9e8fdf8799c` are preserved.
One Human invocation was issued. The invocation stopped during verified-runtime construction before
attempt-claim publication, Docker mutation, PostgreSQL startup, or Migration 1. Therefore V12 is not
described as durably consumed: its claim, consumption marker, SUCCESS receipt, and TERMINAL receipt
are absent, while its Human invocation allowance is independently `EXHAUSTED`. The public gate binds
that source-owned exhaustion authority and returns `NOT_ELIGIBLE` even after process restart. No
claim, marker, or retroactive consumption record is fabricated.

Human invocation allowance and durable execution consumption are separate one-shot dimensions. A
fresh invocation requires both an available Human allowance and a permissive canonical durable-state
evaluation. For a future revision the public bootstrap resolves the exact approval, gates the
invocation, and durably publishes a distinct `human-invocation-issued` artifact before any
verified-runtime snapshot construction. The artifact binds only the digest of a random, ephemeral
continuation capability. The capability itself crosses the sanitized child-environment allowlist but
is never written durably. The verified-runtime executor accepts the otherwise-exhausted issuance only
when that exact capability validates against the canonical issuance readback; a restarted bootstrap
cannot recover or reissue it. This makes snapshot failure and pre-claim process loss permanently
non-runnable while allowing only the already-issued invocation to continue. The issuance artifact is
not the execution consumption marker.

The public bootstrap no longer selects a closure revision independently. The active finite execution
descriptor carries the executable-source closure authority ID. That descriptor selects the exact
closure file set, which determines the source digest, verified-runtime snapshot rows, digest
validation, and public child execution. Current revision 13 qualification is bound to closure V2.
Closure V2 includes the canonical
`scripts/sql/day146_operational_memory_snapshot_persistence.sql` prerequisite through initial-catalog
V2 provenance. A V1 selection for the current descriptor, omission of day146, an extra file, or a
mixed closure is rejected. Historical descriptor identities remain byte-for-byte stable; no
historical closure is promoted into current authority.

Revision 13 is the internal Day150 Gate17 execution authorization that was subsequently Product
Owner approved and executed exactly once. Executable source was frozen at
`sha256:b8a95697a2439a31d180706878ceb1c66171ba563e82037bf18518f382bccfa6`, and the V13 proposal was
derived at `2026-08-20T05:40:44.000Z`. The successful execution is now non-runnable. Its five
candidates were unapproved at execution completion and were later promoted by the separate exact-five
Product Owner approval; production, canonical, B2, and Gate2 operation limits remain zero.

## Day150 V11 reconciliation and V12 source-repair supplement

V11 remains historical `OUTCOME_UNKNOWN`, consumed, non-runnable, and retry
forbidden. Read-only reconciliation proved its surviving database was exactly
after migration 1 and that migration 2 had no committed effect. The observed
migration 2 PostgreSQL error was SQLSTATE `23514`: the canonical guard rejected
the missing `ai.operational_memory_projection_state_events` prerequisite before
the first migration 2 mutation. Exact compensation subsequently removed only
the V11 container, volume, and network and independently proved all three exact
names absent. No candidate, SUCCESS, or TERMINAL artifact was synthesized.

Future reference execution uses
`DAY150_PREFIX_REFERENCE_INITIAL_CATALOG_AUTHORITY_V2`; V1 remains the immutable
historical V11 binding. V2 adds only the structural dependency closure from
`scripts/sql/day146_operational_memory_snapshot_persistence.sql`, whose first
canonical creation is commit `e586de8ab00512ba806447c32dd1217f424e78d7` and
whose current canonical blob is `ca8f3c8954560f3e8abd7c7d5fcf8a44ed6d428f`.
The source SHA-256 is
`017c69c6cbfcf8efbe2cd042c32cfb88a848b6f48d65f23189f47dc22e6cefdc`.
It predates migration 2 and the remaining Gate17 prefix migrations; migration 1
does not require it, and snapshot 1 is the first qualified state that includes it.
The adopted closure is six
operational-memory tables, their append-only enforcement function and six
triggers, the bundle persistence function, and the source-defined indexes and
constraints. Roles, passwords, grants, default privileges, application data,
and unrelated schemas are excluded.

The PostgreSQL worker settlement protocol has a finite domain:
`MUTATION_COMMITTED`, `MUTATION_REJECTED_NOT_COMMITTED`, and
`MUTATION_OUTCOME_UNKNOWN`. A rejected result is trusted only when a structured
SQLSTATE is received inside the exact transaction envelope, COMMIT was not
acknowledged, and ROLLBACK was acknowledged. Arbitrary nonzero exit, malformed
or incomplete response, process loss, timeout, IPC ambiguity, and any possible
commit boundary remain `OUTCOME_UNKNOWN`. This repairs future classification;
it does not rewrite V11.

For the isolated Gate17 reference executor,
`core_schema.migration_history` rows are not adopted applied-state authority and
the executor is not authorized or required to write them. Ordered history is
source-derived metadata. Applied-state evidence is the pinned migration
identity, deterministic settlement, canonical readback/snapshot, candidate
provenance, and ordered source metadata. Therefore an existing empty history
table is not an applied-state contradiction and no history mutation was added.

The load-bearing qualification uses the same rendered worker protocol and exact
PostgreSQL image as REAL execution. It applies the V2 bootstrap and all five
pinned migrations, captures F/G canonical snapshots after each migration, and
keeps all candidate artifacts absent. Both a guard failure before the first
mutation and a transaction failure after transactional DDL are proven rolled
back and classified `MUTATION_REJECTED_NOT_COMMITTED`; unstructured nonzero exit
remains unknown.

The V4 pre-mutation qualification narrative below is retained as historical evidence. It does not
describe current execution authority. The authoritative post-V4 state and V5 proposal are frozen
in the final sections of this document. V4 is terminal and unretryable.

Historical V4 execution gate: `V4_FINAL_PRE_MUTATION_QUALIFIED`.
The fresh independent Sol gate is `P1=0 / P2=0 / GO=YES`. Product Owner V4
narrowly authorizes one immutable canonical attempt-claim artifact at the fixed
path documented below. Source qualification covers a 61-entry attempt-bound REAL
adapter trace, 52 process-loss boundaries, 104 fresh-public-executor cases using
208 executor instances, 2,608 phase-specific reconstruction assertions, actual
schema A-I coverage, and a true two-OS-process ambiguity/recovery case plus the
11-case cross-process negative matrix. Six simultaneous two-process claim races
prove that exactly one executor can consume V4; the loser recovers
`ATTEMPT_CLAIMED` and stops with `OUTCOME_UNKNOWN`. The private actual primitive
also behaviorally proves bounded nonzero process completion, expected Docker
absence interpretation in the REAL adapter, and terminal-close fencing of an
in-flight durable filesystem publication. Qualification state persists in the
repository-external store, so process B receives only the store root and reopens
canonical authority bytes. The complete source suite and fresh independent Sol
gate passed before mutation. No Docker, PostgreSQL, migration, stage, commit, or
push operation had occurred at this pre-execution qualification point.

## Initial catalog authority

The Product Owner adopted
`DAY150_PREFIX_REFERENCE_INITIAL_CATALOG_AUTHORITY_V1`. The repository-owned
contract is implemented in
`src/lib/hermes/farm_os_day150_prefix_initial_catalog_authority.ts`. It adopts
only the exact historical `ai` schema and the exact 19-column
`ai.proposal_inbox` structural definition needed before migration
`202607260001_eligible_proposal_persistence`. Its lineage is the unchanged Git
blob `06629c7fade0b78c129e5f4db578a2a5a1137556`, SHA-256
`645921b4a710450bc2f48aba9908727a5c888fa1125731800399df2d35b7e2c2`,
introduced by commit `a07ce58c7437305da3d3936a1c47bc4a1e16f2e1`.

The complete Day3 artifact is explicitly not adopted. App and knowledge
schemas, unrelated audit prestate, local LOGIN roles, passwords, local grants,
ownership choices, and default privileges are excluded. The initial security
baseline is owner-only: no application/user grant, no development grant, no
explicit PUBLIC privilege, and no development default privilege.

The deterministic compiler produces a source-only plan containing one bounded
NOLOGIN reference role, the `ai` schema, the exact historical base table DDL,
and explicit PUBLIC revocation. The role has no password, superuser, CREATEDB,
CREATEROLE, replication, or BYPASSRLS capability. The plan is not SQL execution
authority and has not been run.

## Semantic principal normalization and additive fingerprint v2

The portable owner/grantor semantic is `REFERENCE_MIGRATION_OWNER`. A reference
raw principal is selected only inside the authenticated reference-run path;
the pure v2 digest helper is not authority or a run capability. An observed raw
principal can normalize only through an opaque binding issued from fixed
repository target-owner authority. A caller-provided mapping, matching string,
unknown target owner, or unapproved owner cannot issue that capability.
The observed-target binding registry is intentionally empty until a separate
target-specific human authority is checked in.

Gate 17 no longer treats a caller-supplied observed digest plus an authority
label as semantic evidence. Exact comparison additionally requires an opaque
observed semantic-fingerprint capability. That capability can be issued only
by the module-private trusted collector path from a repository-approved
target-owner binding and the fixed read-only catalog collector authority. It is
bound to the exact target identity and expires after the bounded freshness
window; Gate 17 rechecks `observed_at <= trusted server-owned governance clock
< expires_at`. Caller-built or backdated clock, ACL, grantor, digest, or
evidence objects fail closed.

Historical `farmos.pg-catalog-fingerprint.v1` parsing and hashing remain
unchanged. Day150 adds
`farmos.pg-catalog-semantic-principal-fingerprint.v2`, whose domain binds the
normalization policy, initial catalog authority and digest, object-universe
revision and digest, catalog-query revision and SHA, and canonicalization
revision. It consumes the existing privacy-qualified commitment snapshot and
normalizes only the principal selected by the authenticated path; fixed Core
role identities remain unchanged. The additive v2 input retains each selected
ACL grantor and binds its normalized semantic identity; unknown principals or
grantors are rejected rather than omitted. `INTERNAL_RAW_NEVER_PERSIST` is not
weakened.

The authenticated reference-run receipt now additionally binds the initial
catalog authority ID/revision, canonical initial-state digest, principal
normalization revision, and generated bootstrap-plan digest. A differently
initialized database or fixture bootstrap therefore cannot authenticate as
this reference run. Completion additionally requires a repository-loaded
revised execution-authorization capability and an opaque module-private exact
collector readback capability covering the 19 columns, four constraints, one index,
owner-only state, zero explicit application/PUBLIC grants, zero credential
statements, and zero unrelated schemas. Successful real execution consumes its
authorization once. V3 is retained only as `SUPERSEDED_UNCONSUMED`; the
executable repository registry contains exactly V4 in
`AUTHORIZED_BUT_NOT_CONSUMED` state and cannot fall back to V3. No claim,
candidate, or receipt artifact exists before the authorized run.

`farmos.day150-prefix-expected-catalog-derivation.v1` compiles deterministic,
human-reviewable expected-catalog candidates. A candidate uses the distinct
`farmos.day150-prefix-expected-catalog-candidate.v1` schema, has
`authority_state=EXPECTED_CATALOG_CANDIDATE`, and keeps both approval fields
`null`. It is never accepted as the historical
`farmos.expected-catalog-fingerprint-authority.v1` approved authority.

The compiler owns the fixed repository inputs: the first five ordered entries
of `db/provisioning/manifest.json`, their apply/verify identities, the v5
catalog query SHA, the complete per-prefix relation/function/role scope, the
catalog canonicalization version, PostgreSQL 17, the exact adopted image
digest, and `linux/arm64/v8`. Its only future reference-run input is the exact
F ACL and G migration-catalog result sets accepted by the existing v2/v5
result contract. The compiler itself derives the catalog objects and accepts
no caller-built object catalog, expected fingerprint, or approval field.

Raw F/G rows are ephemeral input only. The existing production-identity privacy
transformer replaces function bodies, `proconfig`, defaults, definitions, and
policy expressions with domain-separated SHA-256 commitments before the prefix
compiler sees them. Persistable snapshots use
`farmos.day150-prefix-persistable-catalog-snapshot.v1`, declare
`raw_catalog_values_persisted=false`, and contain commitments rather than raw
catalog text. Raw rows never enter a candidate, registry, review packet,
receipt, log, or artifact.

Promotable compilation requires an opaque reference-run capability issued only
from an opaque controlled-executor completion after exact capture validation.
Plain F/G rows, provenance strings, structural receipt objects, production
observations, and the separate qualification-only capability cannot satisfy
this boundary. Qualification produces only
`farmos.day150-prefix-catalog-qualification-result.v1`, which candidate parsers
and registries reject.

The executor-completion issuer is module-private. It exact-validates the
bounded pre-candidate capture and F/G contracts, converts F/G to the safe representation,
and owns the recursively frozen result before issuing an opaque completion.
It intentionally exposes no fixture, collector, or generic-row entrypoint.

Candidate registry creation recompiles all five candidates from the
WeakMap-owned authenticated run results and requires canonical identity with
the supplied artifacts. Matching only the authentic receipt digest cannot
admit caller-rebuilt snapshot or ACL content.

The source-only preflight reads the fixed repository manifest, exact five
apply/verify artifacts, and pinned v5 query bytes. Missing or mismatched bytes
fail closed. It never starts Docker, connects to PostgreSQL, applies a
migration, or writes a candidate artifact.

The five snapshot points are state immediately after each exact migration:

1. `202607260001_eligible_proposal_persistence`
2. `202607300001_daily_operational_projection_candidate_foundation`
3. `202607310001_daily_operational_projection_candidate_activation`
4. `202608030001_daily_operational_projection_command_ledger`
5. `202608070001_stable_changes_consumer_persistence`

Candidate files have fixed paths under
`artifacts/day150/prefix-expected-catalog/candidates/v1/`. No candidate file is
created before the authorized real run.

The bounded final receipt candidate path is
`artifacts/day150/prefix-expected-catalog/reference-runs/v1/reference-catalog-run-receipt-candidate.json`.

## V4 migration privilege envelope, attempt claim, and real executor

`DAY150_PREFIX_REFERENCE_MIGRATION_PRIVILEGE_ENVELOPE_V1` separates the immutable
`REFERENCE_MIGRATION_OWNER` from the disposable `REFERENCE_MIGRATION_EXECUTOR`.
The owner remains `NOLOGIN NOCREATEROLE`; the executor is the only migration
session and is `LOGIN CREATEROLE NOSUPERUSER NOCREATEDB NOREPLICATION
NOBYPASSRLS`. The executor receives membership in the owner plus only `CONNECT`
and `CREATE` on `farmos_day150_prefix_reference_v1`. Before authorization
consumption, the executor reads and digest-verifies every exact migration byte
sequence, owns an immutable in-memory bundle, computes a bundle digest, and
performs statement-by-statement privilege classification. The analysis rejects
database creation, extensions, `ALTER SYSTEM`, external I/O, and any requirement
for superuser or `CREATEDB`. Exact authorization additionally requires the five
repository SHA-256 values, the approved per-migration statement counts, and the
fixed ordered 89-statement privilege-manifest digest; classification alone never
grants authority. Database execution receives only the pinned strings;
migration filesystem reads after consumption are zero.

`DAY150_PREFIX_REFERENCE_EXECUTION_AUTHORIZATION_V4` supersedes unconsumed V3
without consuming it. The only public real initiation function is
`executeFarmOsDay150PrefixReferenceCatalogOnce`; the CLI calls only that function
and accepts no caller configuration. The lower adapter factory can be claimed
only with an unforgeable module-issued real-execution context and cannot
independently mint real authority. Image, platform, Docker names, database, principals,
migrations, query sections, output paths, and cleanup targets are repository
authority. The complete external plan, including nested bootstrap and snapshot
history, is bound by one canonical V4 plan digest; missing, changed, or extra
fields fail closed. A durable consumption marker is created exclusively beside the fixed
receipt path with suffix `.authorization-consumed`, immediately before the
first Docker mutation. Its presence after
process loss is terminal and cannot mint a second authorization.

`SUPPLEMENTAL_ONLY`: the earlier same-process primitive-seam, A-I restart, and
98-case process-loss evidence remains regression-only. V4 load-bearing evidence
uses actual Public Executor A/B creation, actual schemas, 104 fresh-executor
process-loss cases, and the separate OS-process qualification. Documentation is
not upgraded to final completion until the full source gate and independent Sol
review pass.

The target layering is one public executor, one common orchestrator, one
substantive REAL adapter, and a primitive process/byte/filesystem port. The
primitive port contains no PostgreSQL request vocabulary. The REAL adapter owns
PostgreSQL request construction and result parsing. Qualification and future
REAL execution use that same adapter; only the primitive implementation differs.

V4 authorizes exactly one new REAL artifact:
`artifacts/day150/prefix-expected-catalog/reference-runs/v1/reference-catalog-run-receipt-candidate.json.authorization-attempt-claim`,
schema `farmos.day150-prefix-reference-execution-attempt-claim.v1`. No other
`.attempt`, `.claim`, `.reservation`, or `.fence` artifact is authorized. Its
stable run ID and attempt ID are domain-separated derivations of immutable V4
authority, the exact V4 plan, and pinned migration bundle. Publication is
canonical, exclusive, durable, no-overwrite, and requires trusted readback.
Only then may the claim-bound consumption marker be published and read back.
Only the executor receiving direct exclusive-publication success may continue;
preexistence, `EEXIST`, or ambiguous acknowledgement with exact readback recovers
the claim but never grants continuation.
Claim-only or ambiguous-marker restart recovers the original claim identity,
creates no replacement attempt, performs no automatic retry, and remains
`KNOWN_OUTCOME_UNKNOWN_FOR_EXACT_ATTEMPT` unless trusted exact marker readback
proves the same claim was consumed.

The container-local bootstrap administrator may create the two reference
principals, establish exact membership/database privileges, and apply the
qualified minimal initial bootstrap. It cannot be selected as the target
migration session. Each migration executes through a connection authenticated
as the fixed executor and is followed immediately by a repeatable-read,
read-only F/G catalog collection. The five result sets remain distinct.

Each artifact uses an exclusive temporary file, file `fsync`, atomic no-replace
link publication, directory `fsync`, reopen, exact parser, and digest
verification. First the five candidates are durable. A separate `.pre-cleanup`
evidence artifact then binds their identity and artifact digests, exact paths,
the capture digest, and pinned bundle digest. Only after that readback may
cleanup remove the exact owned container, volume, and network and verify all
three absent using exact resource-kind/name Docker absence responses; generic
`not found`, daemon, transport, timeout, and malformed failures do not prove
absence. The final receipt is published only after cleanup and binds both
the pre-cleanup evidence digest and exact cleanup result. Ambiguous
Docker/PostgreSQL/output/cleanup outcomes are terminal:
there is no retry, second authorization consumption, or automatic cleanup.
Every bounded or ambiguous terminal result returns the explicit
`DURABLE_ACTUAL_SCHEMA_READBACK_MANUAL_RECONCILIATION_REQUIRED` handoff and
`NOT_GRANTED_NO_AUTOMATIC_COMPENSATION`; the durable consumption marker plus
actual candidate/pre-cleanup/receipt schemas are the only reconciliation
authority. No new compensation artifact or execution permission is implied.

Semantic fingerprint v3 normalizes only the two trusted infrastructure
principals to distinct `REFERENCE_MIGRATION_OWNER` and
`REFERENCE_MIGRATION_EXECUTOR` identities. Canonical roles created by migrations
remain literal. Fingerprint v1 and the previously qualified single-principal v2
algorithm remain available unchanged. The generated candidate files remain historical source
artifacts after their exact-five Product Owner-approved authority bindings are materialized.
It contains no credential, DSN, argv, environment, or raw catalog value and is
not execution authority or Product Owner approval.

Promotion requires an opaque capability loaded from a repository-owned exact
Product Owner decision. The decision binds the ordered five candidate identity
records, authenticated run-receipt digest, and domain-separated set digest.
Registration owns recursively frozen defensive copies. Validation rejects
duplicates, omissions, additions, reordering, unknown migrations, and changed
identities before producing a whole five-binding set. The repository approval registry contains
exactly one Product Owner decision, and the atomic approved-binding-set registry contains exactly
one ordered five-binding set. All five identities are validated before this repository
representation is accepted; there is no partial or sixth authority. Each approved binding retains
the original candidate identity, its candidate raw file SHA through the promotion basis, corrected
SOURCE v2 evidence, exact V13 SUCCESS lineage, approval reference, and `approved_at`.
Generation success, tests, reference time, or a structurally compatible caller
object cannot issue approval.

## Authorized isolated execution plan

The single conditional V4 run has Product Owner authority and still requires the local
Docker daemon execution boundary to pass. It
uses `docker.io/library/postgres@sha256:7958605b474b3d264a969cb3a123d6aa00ad1e1fe9da8a69984dabb704d93317`
on `linux/arm64/v8`, `--pull=never`, `--restart=no`, one exact owned container,
network, and volume, and a localhost-only ephemeral port. The database is a
clean isolated reference database with ephemeral non-production credentials.

Before the five migrations, the revised plan creates the exact reference-only
NOLOGIN owner, applies the minimal initial bootstrap, and performs structural,
owner-only, PUBLIC-absence, and principal-binding readback. The bootstrap is
prestate, not a sixth prefix. Only after that verification may the existing
five exact migrations run through the reference-only executor, with exact
membership in the owner, and the five exact
migration/snapshot points proceed.

For each snapshot point, it applies the next pinned migration exactly once,
runs the canonical catalog query read-only, compiles the full
object universe, and writes one candidate artifact. Cleanup removes only the
exact owned container, volume, and network and verifies zero residual owned
resources. It has no production target, production credential, canonical
FarmOS root/ledger, B2 authorization, or Gate 2 path.

Before the one-shot command is invoked, Docker, PostgreSQL, migration,
production, and canonical operation counts are all zero.

Resource preexistence remains fail-closed. A bounded Docker inspect completion
proves absence only for exit code 1 with either empty stdout or the exact
observed bounded empty-inspect-array frame (`[]\n`). Stderr normalization removes
at most one terminal LF or CRLF and performs no other whitespace, case, or text
normalization. The result must then equal the exact kind/name-bound container,
network, volume, or explicitly retained legacy `Error: No such <kind>: <name>`
literal. Network and volume additionally recognize exactly one independently
authorized daemon-prefixed literal each: `Error response from daemon: network
<exact-name> not found` and `Error response from daemon: get <exact-name>: no
such volume`. The daemon prefix is part of those literals and is never stripped
or matched generically. Exit code 1 by itself, another resource name, whitespace variants,
additional text, daemon/socket/context/permission/timeout failures, and
output-limit failures do not prove absence.

The three independently classified results use deterministic precedence: any
`PRESENT` blocks resource preexistence; otherwise any `AMBIGUOUS_OUTCOME` remains
ambiguous; otherwise any `BOUNDED_FAILURE` fails closed; only three `ABSENT`
results clear preflight. Thus a confirmed present resource cannot be hidden by
another resource's failure, while ambiguity remains more conservative than a
bounded failure when no resource is present. Traces retain typed per-resource
classifications without raw stderr.

This paragraph records the historical pre-run gate: V2 and V3 were
`SUPERSEDED_UNCONSUMED`, and V4 was `AUTHORIZED_BUT_NOT_CONSUMED`. V4 was subsequently consumed
exactly once. The current state is defined below and this historical paragraph grants no retry.

## V4 terminal evidence and exact compensation

V4 remains `CONSUMED_TERMINAL / EXACTLY_ONCE`; retry is `FORBIDDEN`. Its immutable identities are:

- attempt ID `sha256:93ce91fa84fc02a17274fcac777828dc2ba7f2f5b5c3aae5fd9804bed7b3fe2e`
- run ID `sha256:df00486286c2dcf55d9c116434f08f1dc8f00c164dcd25573c6a6b98391886bd`
- attempt-claim digest `sha256:9029e17fc856fa0a0e7b5c00068d732a2368d2c74f0d00a699963aaf7ce027cf`
- consumption-marker digest `sha256:287a3e0c96d9d9247190d4de16dd06d8c8899671b10b83f76d45f127f8011878`

The exact durable paths remain present and immutable:

- `artifacts/day150/prefix-expected-catalog/reference-runs/v1/reference-catalog-run-receipt-candidate.json.authorization-attempt-claim`
- `artifacts/day150/prefix-expected-catalog/reference-runs/v1/reference-catalog-run-receipt-candidate.json.authorization-consumed`

Immediately before compensation, trusted reconciliation proved container
`farmos-day150-prefix-reference-pg17-v1` had exact ID
`fa4564fff64f9da0d499404826dcb3400fe588b253e7ac3622d1569d2bebd310`, exact image digest
`sha256:7958605b474b3d264a969cb3a123d6aa00ad1e1fe9da8a69984dabb704d93317`,
`restart=no`, and only the expected network and volume. Network
`farmos-day150-prefix-reference-network-v1` had exact ID
`8867c51930bdcab008fd3148a47bfc5333b381d38370a3b23c9891e0bc41ab81` and only that endpoint.
Volume `farmos-day150-prefix-reference-volume-v1` used driver `local`, was created at
`2026-08-15T21:26:15+09:00`, and was attached only to the exact V4 container.

The bounded sequence completed once: graceful container stop, exited-state readback, exact
container removal, exact volume removal, exact network removal. Independent kind/name inspections
then returned the approved exact absence forms for all three resources. Residual V4 Docker
resources are zero, unrelated Docker resources touched are zero, and no V4 durable evidence was
removed or rewritten. Classification: `V4_EXTERNAL_RESOURCES_COMPENSATED`.

## PostgreSQL readiness liveness repair

The repaired load-bearing path remains public executor → common orchestrator → substantive REAL
adapter → bounded primitive system port. The prior defect issued a single immediate `SELECT 1`
after container startup and misclassified the expected startup race as
`POSTGRES_READINESS_PROCESS_FAILURE` even though PostgreSQL became ready about 1.48 seconds later.

`DAY150_PREFIX_REFERENCE_POSTGRES_READINESS_LIVENESS_POLICY_V1` fixes one operational window:
60,000ms maximum, at least 500ms between probes, at most 120 probes, and only read-only `SELECT 1`
through the exact isolated PostgreSQL channel. `ECONNREFUSED`, Docker-proxy startup
`ECONNRESET`/`EPIPE`, PostgreSQL `57P03`, and the selected `pg` client's exact no-code
`Connection terminated unexpectedly` startup disconnect at the already verified endpoint are
retryable startup-transient conditions. The exact client message is normalized to the finite
internal code `PG_CLIENT_CONNECTION_TERMINATED_UNEXPECTEDLY`; arbitrary no-code errors remain
terminal. Authentication, wrong database,
permission, wrong endpoint, malformed result, unknown/nonzero process result, and primitive
channel failures are terminal and fail closed. Exhaustion returns `POSTGRES_READINESS_TIMEOUT`.

The REAL primitive supplies monotonic time and bounded waits; qualification supplies deterministic
virtual time through the same primitive contract. Success must complete strictly before the
60,000ms deadline. Readiness success sets only the readiness gate; independent database/major
readback must still prove the exact database and PostgreSQL major 17 before principal initialization
or migration execution. Multiple readiness probes are not an execution retry: one authorization,
attempt, invocation, container, and PostgreSQL process remain in force, with zero container restart
or recreation authority.

## V5 exact proposal freeze

The exact source-frozen `DAY150_PREFIX_REFERENCE_EXECUTION_AUTHORIZATION_V5` proposal was approved
by the Product Owner on 2026-08-16. Its proposal object and approved digest remain unchanged. The
historical separate one-invocation activation was consumed; the current executable registry is
empty.

- proposal state retained in the digest-bound object: `PROPOSED_NOT_AUTHORIZED`
- activation state before invocation: `AUTHORIZED_BUT_NOT_CONSUMED`
- invocation limit: `1`; automatic retry: `false`
- authorization digest: `sha256:ba779bf325c8d5a5c505f4b9f9d733a77c888d4d3610220a4af86494cc47c3fb`
- external plan digest: `sha256:c470bf3042e9f6f94cab73c0ba33a0c38274b4431ceb1e270b0244cf3cb2108d`
- proposed run ID: `sha256:9b74b615dae04f11febd020db6c5f7004e9ca2f5705a61b06daae47f53bd1b3a`
- proposed attempt ID: `sha256:488f06a42fd070ab158ec7e228527e220104a0d213b6829550e9c66c32566fb6`
- V5 attempt claim: `artifacts/day150/prefix-expected-catalog/reference-runs/v1/v5/reference-catalog-run-receipt-candidate.json.authorization-attempt-claim`
- V5 consumption marker: `artifacts/day150/prefix-expected-catalog/reference-runs/v1/v5/reference-catalog-run-receipt-candidate.json.authorization-consumed`

The two V5 paths do not exist before invocation. They are the complete pre-candidate run-specific durable
identity set required by the existing claim/marker convention; no extra attempt, reservation, or
fence artifact is proposed. Both paths remain under the adopted reference-run namespace, are exact
and versioned, and preserve canonical exclusive durable no-replace publication semantics.

The V5 external plan changes only the distinct V5 durable identities and the readiness liveness
policy. It retains the same PostgreSQL image digest, `linux/arm64/v8`, `--pull=never`,
`--restart=no`, localhost ephemeral exposure, reference-only database/principals, initial catalog
authority, fixed five migrations and five snapshots, candidate paths, final receipt semantics,
fixed 89-statement privilege manifest, and exact zero-residual cleanup scope. The compensated Docker
names may be reused only because exact absence has been proven. Production, canonical, B2, Gate 2,
candidate generation, migrations, and execution remain outside this proposal gate.

The exact Product Owner request that has now been approved was:

> Review `DAY150_PREFIX_REFERENCE_EXECUTION_AUTHORIZATION_V5`, authorization digest
> `sha256:ba779bf325c8d5a5c505f4b9f9d733a77c888d4d3610220a4af86494cc47c3fb`, and external plan digest
> `sha256:c470bf3042e9f6f94cab73c0ba33a0c38274b4431ceb1e270b0244cf3cb2108d`. If and only if you intend
> one new isolated reference invocation under those exact frozen terms, explicitly authorize V5.

The exact human decision now exists for one invocation only. No external mutation is permitted
until the V5 claim and consumption marker have each been durably published and trusted-read back.

## Protected state at this gate

The protected-six start evidence is retained exactly:

- `coordination.lock`: SHA-256 `13ccf2b4e00eb245805a8d525029bbb80ae5d6761924ae0e1f32a1e9b20adda5`
- `tsconfig.tsbuildinfo`: SHA-256 `5238a5fa72b92fb951cfff46bba595004f7705d0fb302186c4c95a66cdba12e7`, size 210111, mtime `2026-08-06T14:28:11+0900`
- runtime SQL: SHA-256 `71c5003048a687104cdb82190c353c09112a8b71da8f4d68ead906a9f76701e9`
- rollback SQL: SHA-256 `cc426dd8e60d751d9b79604ccc3dc774319af83d6329aa9cf97fe880c377ba58`
- V4 attempt-claim file: SHA-256 `fbd94748b9c39a666120a440f7e606122fe22316eb0d44b5d1b8699bbd41a4b0`
- V4 consumption-marker file: SHA-256 `9c01aaa44c07ba0fc648a9c71b4ec0a02dc422b5dff532fd1e886d916407615e`

Repository baseline remains branch `main`, HEAD/origin
`ffcc2facbf51f88dda8ce2f0258b92d861367339`, ahead/behind `0/0`, staged files zero.

## V5 immutable terminal history and compensation

V5 is permanently `CONSUMED / EXACTLY_ONCE`; retry and automatic continuation are forbidden. Its
historical caller classification remains `OUTCOME_UNKNOWN`. The exact V5 claim and consumption
marker are immutable and no terminal-outcome receipt is retrofitted. A final trusted reconciliation
proved PostgreSQL 17 and the reference database existed, while reference principals, initial
bootstrap, all five migrations, candidates, pre-cleanup evidence, final success receipt, and
approved expected-catalog authority were absent. The exact owned container, volume, and network
were then removed once and independently verified absent. External state is therefore
`V5_EXTERNAL_RESOURCES_COMPENSATED`; that does not rewrite the historical caller result.

## Additive terminal-outcome receipt authority

`DAY150_PREFIX_REFERENCE_TERMINAL_OUTCOME_RECEIPT_AUTHORITY_V1` owns only schema
`farmos.day150-prefix-reference-terminal-outcome-receipt.v1` and only non-success terminal outcomes.
The existing `farmos.day150-reference-catalog-run-receipt-candidate.v1` success authority remains
exact-five, cleanup-complete, and zero-residual; its parser and digest domain do not accept the new
failure receipt.

For one authorization, run, and attempt, at most one terminal receipt class may be authoritative:
the existing success receipt XOR the additive terminal-outcome receipt. Both publications are
exclusive/no-replace. Each publication checks the other exact path first. A trusted existing
opposite receipt stops publication, while two present receipt classes or an invalid existing
receipt fail closed for read-only reconciliation. The exclusive attempt claim permits only one
authorized publisher; concurrent or restarted executors that lose that claim remain read-only.
Terminal publication and readback use the distinct audit boundaries
`TERMINAL_OUTCOME_DURABLE_PUBLICATION` and `TERMINAL_OUTCOME_REOPEN_READBACK`; they never reuse or
impersonate the success receipt's `FINAL_RECEIPT_*` lineage.

A deterministic terminal failure is returned only after claim and marker trusted readback and a
successful terminal-close settlement fence, followed by canonical terminal receipt construction,
exclusive durable publication, reopen, parser validation, digest validation, and exact binding
validation. A failed or ambiguous close fence leaves the outcome unknown and publishes no
deterministic terminal receipt. The receipt binds authorization and plan digests,
run, attempt, claim, marker, last trusted phase, finite terminal class and failure code, zero through
five exact durable candidate identities, pre-cleanup presence/digest, finite cleanup state,
independent zero-residual state, bounded observation provenance, and its canonical digest. It
persists no credential, DSN, argv, environment, raw PostgreSQL output, stderr/stdout, or
`INTERNAL_RAW_NEVER_PERSIST` value.

Cleanup states are `NOT_STARTED`, `PARTIAL`, `COMPLETED`, or `AMBIGUOUS`. Zero residual is separately
`NOT_VERIFIED`, `VERIFIED`, or `AMBIGUOUS`; cleanup attempt never implies absence. Terminal classes
are finite: `TERMINAL_FAILURE`, `OUTCOME_UNKNOWN`, and `COMPENSATED_TERMINAL_FAILURE`. Success is not
in this vocabulary. If publication or readback is ambiguous, the caller receives
`OUTCOME_UNKNOWN`, no automatic retry occurs, and later readback may recognize the exact durable
receipt without rewriting the earlier caller observation.

Fresh public executors reopen the canonical terminal receipt together with the exact claim and
marker. They validate every binding before inspecting residual resource state and return only the
trusted terminal reconstruction; process memory contributes no authority and the attempt can never
become runnable again.

## V6 consumed terminal history and binding/readiness repair

The Product Owner authorized exactly one invocation of
`DAY150_PREFIX_REFERENCE_EXECUTION_AUTHORIZATION_V6`. That invocation is now
`CONSUMED_TERMINAL`; retry is permanently forbidden and the executable authorization registry is
empty. It stopped at `POSTGRES_STARTUP` with `POSTGRES_READINESS_PROCESS_FAILURE`, generated zero
candidates and no success receipt, and performed no promotion. Its immutable defective claim,
marker, and terminal receipt remain historical evidence and are never rewritten.

- authorization digest: `sha256:e0449f2da625de2d509a1133f9b3b34fcd347502be9a257585678f1629eae60f`
- external plan digest: `sha256:ac7f3d8c1caca2f10a273b25ecab0846bf43f2344ae6d2ed0aa285cec7057be5`
- proposed run ID: `sha256:76b73694c7f030d4f6a36562781120f490680fc77414d6485f71f957c5bff6cf`
- Product Owner-approved attempt ID: `sha256:3923ae30523752ee0834f69ca47b8018ece0bc09fa955c0e4fc245c2b815e419`
- defective persisted attempt ID: `sha256:22072724e4031f8a7882ce6b3a3e1b1f23788bdb1409a2fc8d3e3a94b5a0e5ad`
- attempt claim: `artifacts/day150/prefix-expected-catalog/reference-runs/v1/v6/reference-catalog-run-receipt-candidate.json.authorization-attempt-claim`
- consumption marker: `artifacts/day150/prefix-expected-catalog/reference-runs/v1/v6/reference-catalog-run-receipt-candidate.json.authorization-consumed`
- success receipt: `artifacts/day150/prefix-expected-catalog/reference-runs/v1/v6/reference-catalog-run-receipt-candidate.json`
- terminal-outcome receipt: `artifacts/day150/prefix-expected-catalog/reference-runs/v1/v6/reference-catalog-terminal-outcome-receipt.json`

Canonical byte reconciliation proves that authorization digest, plan digest, and run ID match the
approved V6 values in all three artifacts; only attempt ID differs. The artifact file SHA-256 values
are claim `df139b4ea05b19b6d81f6f91ccfaf4522cf401e151676df9822a2de70bb07321`, marker
`6276fe71c1eb6c9d488a9680b9eaa2ee979891edad64b31c32965ee93fb30b8a`, and terminal receipt
`44ced707d509f5fd8cebbb16f50a8d537f089a78ff14405f906be8ff9f3b11d0`. Their internal claim digest
is `sha256:042dc21db4c7caa35d5b3431e54c96c5fab5d9b2c28a7f8092dc3ccddc71afaf`, marker digest is
`sha256:420ce7dca11f4aaf04213f85f8e8836ad0f67d97aa3eef072a0bcb4c09eaf776`, and terminal receipt digest
is `sha256:5e8ae6ac470d8ca6c363be6e5949225c1b5ddc9be6390f4b0b9ed9587102355f`. The terminal receipt binds
last phase `RESOURCE_SETUP_COMPLETED`, class `TERMINAL_FAILURE`, code `POSTGRES_STARTUP_FAILED`,
candidate count zero, cleanup `NOT_STARTED`, and zero residual `NOT_VERIFIED`.

The first identity divergence was in the runtime attempt derivation: it combined the approved V6
authorization, plan, run, and pinned migration bundle with the stale domain
`farmos.day150-prefix-reference-v5-attempt-identity.v1`. The proposal used the intended
`farmos.day150-prefix-reference-v6-attempt-identity.v1`. The former deterministically produces the
persisted `sha256:220727...`; the latter produces the approved `sha256:3923...`.

`FARM_OS_DAY150_PREFIX_REFERENCE_EXECUTION_DESCRIPTORS` is the immutable finite source for V4, V5,
V6, historical V8, and historical terminal V9 authorization revision, authorization/plan/run/attempt digest domains, durable claim,
marker, success, terminal and candidate paths, schema revisions, pinned plan identity, and
PostgreSQL application name. The selected descriptor, exact activation, and exact external plan are
joined by `FARM_OS_DAY150_PREFIX_REFERENCE_ACTIVE_EXECUTION_BINDING`. Both proposal qualification
and the REAL execution adapter consume that same binding. Binding validation recomputes the plan
digest and rejects authorization, revision/domain, run/attempt, schema, candidate, and every
durable-path mismatch before execution. The stale V5 derivation remains reachable only by an
explicit historical regression fixture. The generic receipt parser can preserve historical V6
bytes, while exact V6 authority parsing rejects the defective attempt binding.

Read-only Docker reconciliation found container
`373e63120a86bb5dcc891c6cbe595eed4671d082eb30eeb667469b2aba178bb3`, created at
`2026-08-15T22:35:15.95285468Z`, started at `2026-08-15T22:35:16.010594609Z`, still running the exact
approved PostgreSQL digest with restart policy `no`, localhost port `32770`, and only the exact V6
network and volume. The volume and network were created in the same claim/marker second and have
only the expected attachments. Together with pre-execution all-three absence, one invocation, and
zero retries, this established `V6_RESOURCE_OWNERSHIP_RECONCILED`. A later exact Product Owner
compensation authorization removed only that container, volume, and network. Every mutation had an
unambiguous acknowledgment and each exact name was independently read back absent. V6 external
state is `V6_EXTERNAL_RESOURCES_COMPENSATED` with `ZERO_RESIDUAL = TRUE`; claim, marker, terminal
receipt, and the defective historical attempt binding remain byte-exact.

Bounded logs prove PostgreSQL 17 completed initialization and became ready at
`2026-08-15T22:35:17.257Z`, about 1.25 seconds after container start. The terminal receipt was
published in the preceding wall-clock second. The REAL loop was present, but its transient
classifier admitted only `ECONNREFUSED` and `57P03`; an unrecognized startup-phase process result
was therefore classified terminal before the database became ready. The exact probe code, probe
count, and per-probe timing were not persisted, so it is not claimed that the observed code was
specifically `ECONNRESET` or `EPIPE`; `READINESS_TRACE_NOT_DURABLY_AVAILABLE` remains the truthful
trace classification. The bounded classifier now admits both Docker-proxy startup forms, and source
qualification exercises connection-refused, server-starting, connection-reset, and broken-pipe
startup transients through the actual public executor, common orchestrator, REAL adapter, and
primitive seam, without container restart or authorization retry. The orchestrator's outer typed-
effect settlement deadline is explicitly 65,113ms, exceeding the 60,000ms REAL readiness window;
a load-bearing assertion prevents the outer fence from silently truncating that policy.

## Gate17 execution proposal lineage

The Product Owner-adopted `DAY150_GATE17_MINIMAL_SCOPE_AUTHORITY_V1` is the scope authority for
this proposal. It fixes the active business path to isolated PostgreSQL 17, the minimal initial
catalog, the exact five pinned migrations and snapshots, five candidate artifacts, bounded cleanup,
independent zero-residual verification, and a separate exact-five Human promotion boundary. It
keeps production, canonical, B2, and formal Gate 2 operations at zero. The authorization and plan
bind the scope-authority digest, and the executable source closure includes its repository source.

Hostile same-UID substitution, a root-owned fixed Node tree, an immutable Docker CLI trust root,
administrator installation, and the sealed-bundle candidate are
`FUTURE_UNASSIGNED_DEFENSE_IN_DEPTH`. They are not imported by the active source closure and are
not Day150 completion gates. This does not weaken exact approval binding, durable one-shot and
replay rejection, restart reconstruction, claim/marker and receipt consistency, stale approval
rejection, or the Human exact-five promotion boundary.

The paragraph that formerly identified
`DAY150_PREFIX_REFERENCE_EXECUTION_AUTHORIZATION_V8` as the current proposal is historical. V8 was
subsequently authorized, invoked once, and exhausted before durable claim publication; it is now
historical and non-runnable. V7 is retired historical and non-runnable. V9 was subsequently
approved, invoked once, and ended terminal; it is consumed, non-runnable, and retry-forbidden.
V10 was approved but never materialized or invoked; its source identity became stale when the
public-bootstrap binding defect was repaired, so it is now pre-invocation stale and non-runnable.
V11 was subsequently approved, invoked once, and reached an `OUTCOME_UNKNOWN` terminal historical
state after Migration 1; it is consumed, retry-forbidden, and non-runnable. V12 was then approved and
issued exactly once, but stopped before durable execution consumption; its Human allowance is
exhausted and it remains non-runnable. Revision 13 is the active descriptor and has exactly one
`PROPOSED_NOT_AUTHORIZED` proposal, but no V13 Product Owner approval or invocation. V4/V5/V6 remain immutable
historical descriptors; none can be selected by the public executor or REAL adapter. No historical
revision name exports the active V13 body, digest, closure authority, or durable paths.

`DAY150_PREFIX_REFERENCE_EXECUTABLE_SOURCE_CLOSURE_V1` is derived from a deterministic explicit
runtime root set. The closure walker recursively resolves every repository-local static runtime
import (including literal dynamic imports), sorts the reachable paths, and adds only the explicit
runtime data dependencies. A completeness gate compares that derived set to the declared closure
and fails on either omission or unexpected broadening. The production identity V5 adoption source
is therefore load-bearing and cannot be omitted without a gate failure.

`package.json` and `pnpm-lock.yaml` are exact repository package authority and are included in the
same digest; `node_modules` and installed-package timestamps are not. The remaining explicit data
dependencies are the provisioning manifest, the consumed read-only query, and the five pinned
migration/verification pairs. `DAY150_PREFIX_REFERENCE_TSX_CONFIG_AUTHORITY_V1` explicitly selects
`tsconfig.json`, mechanically resolves every repository-local `extends` dependency, and adds that
sorted config closure to the same source digest. Missing, malformed, substituted, or digest-mismatched
config fails closed. The closure contains rules and behavior but no embedded final approval data.
Its digest is computed from sorted path plus byte-digest rows.

The authoritative process does not inherit `TSX_TSCONFIG_PATH`. A bounded child environment removes
the ambient value and reconstructs it as the exact `tsconfig.json` path inside the verified runtime
snapshot. tsx 4.22.4 reads that value at loader initialization, so config selection occurs in a new
process and is never changed after loader startup. A conflicting ambient value is load-bearing
qualification input and cannot change the selected config or execution result.

`DAY150_PREFIX_REFERENCE_VERIFIED_RUNTIME_SNAPSHOT_V1` is a private, per-invocation, no-overwrite
directory below the repository solely so Node package lookup continues to reach the package/lock-
qualified installation. Every closure path is traversal-checked, opened with `O_NOFOLLOW`, required
to be a regular file, read exactly once, and copied with exclusive creation. The digest is computed
from those exact read bytes, compared with the approval-bound digest, and then independently read
back from the snapshot. Directories are private/read-only before execution. Any missing, extra,
changed, or symlink-substituted snapshot path rejects before the child is spawned. The child entry,
tsx config, static module graph, and substantive REAL adapter dynamic import all resolve inside the
same snapshot; there is no fallback lookup to the mutable repository source tree. The temporary
snapshot is removed after the bounded child exits and is neither canonical authority nor a durable
FarmOS artifact.

Race qualification copies the complete repository-local runtime closure, snapshots source/config A,
then replaces the original REAL adapter or original tsconfig with B before child startup. The actual
repository approval loader, public executor, common orchestrator, REAL adapter, and primitive seam
still complete the success and terminal lineage from snapshot A. Separate pre-snapshot mutations
change the digest and require a new proposal. Source-byte tamper, config-byte tamper, symlink
substitution, missing files, and extra module-capable paths all fail trusted readback.

Human approval data lives separately in the declarative
`reference-execution-approval-registry.json`; that data path is excluded from the executable source
digest, while the code that parses, validates, looks up, and selects approval remains inside the
closure. An approval record binds authorization ID/revision, executable source digest,
authorization and plan digests, run and attempt IDs, proposal/candidate identities,
`proposal_created_at`, execution-descriptor revision/digest, exact bounded external-plan identity,
finite approval reference, `approved_at`, approval-record schema/revision, and its own canonical
domain-separated digest. Missing, malformed, stale, duplicate, conflicting, or partially matching
records fail closed. Same ID and revision never transfer approval to recomputed source or package
authority.

`DAY150_PREFIX_REFERENCE_APPROVAL_TEMPORAL_ORDER_V1` requires canonical UTC and only the ordering
`proposal_created_at <= approved_at <= repository_loader_observed_at`. It adds no approval,
capability, or execution TTL. Proposal creation time is stable approval data, never recomputed from
"now" and never embedded in executable source. Repository observation time comes from the bounded
loader clock port; qualification injects deterministic time through that same port. Filesystem
mtime, Git timestamps, and process-memory timestamps have no authority.

The load-bearing qualification path materializes exact qualification proposal and approval bytes
under an isolated repository root at the same relative approval-data path. The actual file loader,
byte format, parser, canonical approval digest, loader clock, exact lookup, and active selector are
then used before entering the public executor, common orchestrator, substantive REAL adapter, and
primitive seam. No pre-parsed approval registry is injected and process memory supplies no approval
authority. Durable restart reports
`PUBLIC_EXECUTION_FROM_REPOSITORY_AUTHORIZED_ACTIVE_EXECUTION`; the former current V4 wording is no
longer load-bearing.

Before its rejected invocation and retirement, the V7 descriptor, proposal, external plan, approval
candidate, and REAL adapter shared one active binding. Its exact authorization, plan, run, and
attempt domains remain preserved as V7-specific historical evidence. Claim, marker,
success receipt, and terminal receipt use the distinct `reference-runs/v1/v7/` namespace.
Claim and marker explicitly bind approval reference, candidate/proposal identity,
`proposal_created_at`, `approved_at`, and approval-record digest. Both terminal classes bind the
same lineage, so fresh reconstruction accepts only one exact proposal-to-approval-to-claim-to-marker
to-success-or-terminal chain.
Candidate output identities remain the five existing no-overwrite `EXPECTED_CATALOG_CANDIDATE`
paths and remain subject to a separate exact-five Human approval.

The adopted external scope carried historically from V7 into V8, and unchanged into historical V9,
is: the exact pinned
PostgreSQL image, `linux/arm64/v8`,
`--pull=never`, `--restart=no`, localhost ephemeral exposure, the isolated reference database and
two reference principals, minimal initial bootstrap, five migrations/snapshots/candidates, success
XOR terminal receipt, exact owned-resource cleanup, and zero-residual verification. Invocation
limit is one, automatic retries are zero, and production, canonical, B2, and Gate 2 operations are
zero. V7 claim, marker, and receipt remain absent after its rejected invocation. Before its one
Human approval and exhausted invocation, V8 could not create any artifact or perform an external
operation without a complete Product Owner approval record. V8 is now historical and non-runnable.
V9 was approved and issued once, is now terminal and non-runnable, and cannot be revived by this
readiness repair. Any later execution requires a separately adopted fresh authorization revision.

## V7 rejected-invocation reconciliation

The one authorized V7 invocation returned `EXECUTION_AUTHORIZATION_REJECTED` before an attempt
claim, consumption marker, external mutation, or candidate existed. Read-only reconciliation
proved that every Product Owner approval binding matched the then-approved executable source digest
`sha256:efc9ae9f354973eb48ea0abee41a16343c02cf84f532d94aeaec725ca22448f5`.
The approval record was valid; approval temporal ordering and proposal reconstruction were not the
rejecting boundaries.

The first rejecting check was the repository-authorized-runtime predicate in
`executeFarmOsDay150PrefixReferenceCatalogOnce`. It compared `resolve(verifiedRuntimeRoot)` with
the unnormalized result of `fileURLToPath(REPOSITORY_ROOT)`. Because `REPOSITORY_ROOT` is a
directory URL, its path retains a trailing separator, making the strict comparison false. The
loader consequently selected the verified runtime snapshot as its repository root even though
approval data is intentionally excluded from that snapshot. The approval loader therefore read no
registry and returned no execution capability.

The repaired predicate normalizes both filesystem identities before equality comparison and keeps
the separately preserved approval repository distinct from the verified runtime snapshot. This
repair does not revive V7. `DAY150_PREFIX_REFERENCE_V7_REJECTED_INVOCATION_RETIREMENT_V1` records
the exact rejected authorization/plan/run/attempt/source/approval identities and makes every
revision-7 approval ineligible to issue a public execution capability. Qualification-only
capabilities remain isolated test authority and cannot override that public retirement. V7
invocation count remains one, retry remains forbidden, and the exact historical approval record
remains unchanged. Because the repaired executable bytes have a new source digest, the historical
V7 record also fails exact candidate validation. Any later real execution requires a new explicit
Product Owner authorization after a separately adopted execution revision.
The failed process did not persist its `repository_loader_observed_at`; only the production clock
source (`new Date().toISOString()`) and its canonical-output contract are recoverable. That missing
diagnostic timestamp did not cause rejection because approval bytes were unavailable at the
earlier repository-root selection boundary.

## Historical V8 repository approval source-of-truth repair

At the time of that repair, V8 replaced the V7-only approval path with one revision-aware active
binding while leaving
the historical V7 registry bytes unchanged and non-selectable. The load-bearing active descriptor,
authorization proposal, external plan, approval candidate, repository materializer, parser,
canonical digest validator, selector, loader, and public gate now agree on revision 8 and the
`reference-runs/v1/v8/` no-replace durable namespace. Revision 7 remains explicitly retired and
cannot be selected as a fallback.

The V8 approval candidate uses
`farmos.day150-prefix-reference-execution-approval-candidate.v2`; the approved record uses
`farmos.day150-prefix-reference-execution-approval-record.v2` with approval-record revision 2.
The version change preserves the meaning and bytes of historical V7 records. V8 adds the exact
`gate17_scope_digest` as a direct canonical field. It therefore changes the candidate identity and
final approval-record digest, is required by the parser and exact candidate comparison, and must
equal the adopted Gate17 digest
`sha256:5ca2bf142fe5d22af62e6aecd1db3ce2296b531a36891c9ff7d7f48d704cec01`.

The same scope digest is copied into and validated by the attempt claim, consumption marker,
pre-cleanup reference capture, success receipt, and terminal-outcome receipt. Mutation at any link
invalidates that artifact's canonical digest or exact lineage comparison. Qualification writes
approval bytes only beneath an isolated repository authority root, loads them through the real
repository path, and reaches the public authorization gate with a fake primitive effect port;
Docker, PostgreSQL, migration, candidate, production, canonical, B2, and Gate 2 operations remain
zero until a complete Human-approved V8 record exists.

The approval registry is multi-generation. Its array may preserve canonical historical approval
records beside the one current approval record. Selection never uses array order, highest revision,
or timestamps as a preference rule. For the exact active proposal, every record claiming its
authorization ID or revision must validate against the complete candidate and canonical approval
digest; exactly one such eligible record is required. Zero eligible records rejects authorization,
and duplicate or conflicting current records fail closed. Canonical older records are retained as
history and cannot become current authority merely by being present.

The primary repository qualification materializes two records through the actual approval data
path: the exact preserved V7 approval-record v1 bytes followed by one qualification V8
approval-record v2. The loader parses the whole registry, proves V7 non-selectable under
`DAY150_PREFIX_REFERENCE_V7_REJECTED_INVOCATION_RETIREMENT_V1`, selects the sole exact V8 record,
and reaches the public gate without external effects. A V8-only fixture is not the coexistence
gate. Historical-only, invalid/stale/wrong-scope/wrong-proposal V8, duplicate exact V8, conflicting
V8, and unrelated canonical historical record cases are separately rejected or selected as
appropriate.

## Retired V7 historical evidence reconciliation

`DAY150_PREFIX_REFERENCE_RETIRED_EXECUTION_HISTORY_V1` implements the Product Owner's
no-fabrication historical representation decision. A bounded read-only recovery inspected the
exact approval registry, Day150 run artifacts, architecture evidence, the sealed-runtime bundle
and manifest, and every repository-owned Git checkpoint tree containing the V7 source. Two trees
predated V7. The only tree containing V7 and the sealed bundle recomputed source, authorization,
plan, run, attempt, descriptor, external-plan, and candidate identities different from the pinned
approved V7 record. It is later evidence, not the original historical body. No exact V7
authorization body, plan body, or closure manifest reproducing all pinned V7 identities was
durably preserved.

V7 is therefore represented as `BODY_NOT_DURABLY_PRESERVED`, not as a reconstructed proposal. The
opaque record contains only the exact source, authorization, plan, run, attempt, Gate17 scope,
proposal, approval-record, and retirement facts that survived. Its `authorization_body`,
`plan_body`, and `closure_manifest` are explicitly `null`; requesting an executable V7 body or
validating an alleged V7 plan fails closed. No current closure file list, file count, authorization
object, plan object, or executable descriptor is exported as historical V7 evidence.

This opaque mode is historical audit evidence only: V7 remains retired, non-runnable,
non-selectable, invoked once, and retry-forbidden. Current source changes cannot rewrite its pinned
facts. V8 did not inherit an opaque V7 body. Its historical authorization, plan, source binding,
descriptor, proposal, and approval candidate remain exact then-source-derived evidence. V9 retains
its exact approved and terminal historical identities. V10 retains its exact stale, uninvoked
source and descriptor identity. Current V11 has its own complete
current-source-derived objects whose canonical digests are recomputed and validated through the
actual repository loader and public gate.

## Verified-runtime child environment authority

The verified-runtime child is created with an exact six-key environment. No parent key is
copied by name or spread into the child:

- `FARM_OS_DAY150_APPROVAL_REPOSITORY_ROOT` — constructed from the verified repository root.
- `FARM_OS_DAY150_VERIFIED_RUNTIME_ROOT` — constructed from the private verified snapshot.
- `FARM_OS_DAY150_VERIFIED_RUNTIME_SOURCE_DIGEST` — constructed from snapshot readback.
- `TSX_TSCONFIG_PATH` — constructed from the source-bound snapshot configuration.
- `TMPDIR` — fixed to `/private/tmp`, the bounded macOS qualification/process-loss store root.
- `__CF_USER_TEXT_ENCODING` — constructed from the execution UID and fixed UTF-8 process
  encoding because macOS inserts this process-locale key at launch even when it is omitted.

`NODE_OPTIONS`, `NODE_PATH`, ambient `TSX_TSCONFIG_PATH`, preload/loader/instrumentation
configuration, API keys, credentials, connection strings, locale, and temporary-directory
configuration are not inherited. No parent value controls the temporary root or constructed
macOS locale key. The
child starts through exact `process.execPath`; its module
and TSX configuration are source-bound. Docker subprocesses receive a separate fixed `PATH` and
`NODE_ENV` profile. PostgreSQL subprocesses receive only their executor-constructed `NODE_ENV`
and `PGHOST`/`PGPORT`/`PGDATABASE`/`PGUSER`/`PGPASSWORD` envelope. Those subprocess profiles do
not expand the verified child environment.

The public verified-runtime spawn path validates the exact key set before creation. Missing
required constructed values and extra ambient keys fail closed. Parent attempts to replace
executor-constructed values are ignored before validation.

## V8 attempt-claim publication failure reconciliation

The single authorized V8 invocation reached `ATTEMPT_CLAIM_DECISION` and failed at
`ATTEMPT_CLAIM_PUBLICATION` with `PRIMITIVE_BOUNDED_FAILURE`. The real adapter had derived its
artifact repository root from `import.meta.dirname`. Inside the verified-runtime child that path
is the private source snapshot, not the separately preserved approval repository. The claim
primitive therefore attempted recursive parent creation below the mode-`0500` snapshot root and
the first operation, parent `mkdir`, failed with `EACCES` before temporary-file creation, write,
file sync, link publication, directory sync, or trusted reopen/readback.

The repaired adapter continues to load executable code only from the verified snapshot, but
resolves every repository evidence artifact from the normalized
`FARM_OS_DAY150_APPROVAL_REPOSITORY_ROOT`. Mixed or non-normalized root state fails closed. The
actual claim path remains
`artifacts/day150/prefix-expected-catalog/reference-runs/v1/v8/reference-catalog-run-receipt-candidate.json.authorization-attempt-claim`.
Its `v8` parent is intentionally not pre-materialized repository authority: the durable
publication primitive creates missing parents, writes and syncs a same-directory temporary,
links it into the final no-replace name, syncs the directory, removes the temporary, and the
executor then performs canonical trusted reopen/readback before marker publication or mutation
eligibility.

This repair does not make V8 runnable. V8 has one issued invocation, so its human-authorized
invocation allowance is exhausted even though its durable authorization consumption state is
`AUTHORIZED_BUT_NOT_CONSUMED`. V8 retry is forbidden. Its claim, marker, success receipt, and
terminal-outcome receipt remain absent and are not reconstructed. Any real execution requires a
fresh Product Owner authorization revision after this source repair and qualification; this gate
does not generate that revision.

## Finite executable revision dispatch

Executable descriptor validation has a closed revision domain:

- revision 4 uses V4 digest domains and durable-path semantics;
- revision 5 uses V5 semantics;
- revision 6 uses V6 semantics;
- revision 8 preserves its exact registered historical descriptor semantics but its issued Human
  invocation allowance is exhausted, so it is not runnable again;
- revision 9 preserves the exact executed historical V9 descriptor semantics but its terminal
  receipt permanently prevents another invocation;
- revision 10 preserves its exact stale pre-invocation descriptor but has no invocation or approval
  materialization authority;
- revision 11 preserves exact consumed historical audit identity but is non-runnable and cannot be
  proposed or approved again;
- revision 12 preserves exact approved historical identity, but its one Human invocation allowance
  is exhausted before durable consumption and it is non-runnable;
- revision 13 is the sole successful historical descriptor, is permanently non-runnable, and its
  consumed execution approval is distinct from the later exact-five catalog promotion;
- revision 7 and every other revision are rejected as executable descriptors.

There is no latest-version, V12, or V1-closure fallback. V7 is accepted only by the separate opaque retired
historical parser, which preserves pinned audit evidence but exposes no executable body. Each
accepted revision is also bound to its exact registered authorization, plan, run, attempt,
migration bundle, durable paths, receipt-presence shape, schema revisions, and PostgreSQL
application name. A path merely sharing the expected version-directory prefix is not authority.
The repository invocation gate checks historical revisions 8, 9, 11, and exhausted revision 12,
stale revision 10, and active qualification revision 13 explicitly; every other runtime number
returns fail-closed `NOT_ELIGIBLE` and cannot select V13. The legacy V8 proposal-request
compatibility API exposes only `EXHAUSTED_NON_RUNNABLE` historical status, the pinned V8 descriptor,
and null proposal/approval/active-binding fields. It cannot materialize approval or export the
active V13 binding. The V9 compatibility surface likewise exposes only terminal consumed history,
with null proposal, approval materializer, and active binding.

## V9 historical terminal authorization and readiness repair

V9 was a narrow internal Gate17 execution-authorization revision. Its one approved invocation is
consumed and terminal; it is non-runnable and cannot be retried. It did not change the fixed
Gate17 scope, migration bundle, five candidate contract, PostgreSQL 17 image, privilege envelope,
readiness policy, cleanup policy, or zero production/canonical/B2/Gate2 authority. Its claim,
marker, SUCCESS receipt, and TERMINAL receipt use distinct paths below
`artifacts/day150/prefix-expected-catalog/reference-runs/v1/v9/`.

The load-bearing qualification registry contains, simultaneously, the exact opaque retired V7
record, the exact approved-but-exhausted V8 record, and one exact qualification V9 record. The
selector chooses exactly one approval for the explicitly requested V9 candidate; it does not use
record order, timestamps as preference, total record count, or a latest-version rule. Missing,
stale-source, wrong-scope, duplicate, and conflicting V9 approvals fail closed. The public
repository invocation gate classifies the exact historical V8 record as `NON_RUNNABLE` because
`HUMAN_INVOCATION_ALLOWANCE_EXHAUSTED`; absent V8 claim and marker artifacts do not restore retry
authority.

The V9 executable code was loaded from the verified runtime snapshot while all approval, claim,
marker, and receipt evidence resolves from the separately normalized approval repository root.
Approval eligibility and durable-state observation are separate results. The public gate always
reads the exact derivable claim, marker, SUCCESS, and TERMINAL paths through the same canonical
artifact evaluator used by qualification restart reconstruction and REAL-adapter recovery. Only an
`ENOENT` trusted read is `ABSENT`; inaccessible, non-canonical, malformed, or failed readback is
`AMBIGUOUS`, and well-read but wrong-lineage content is `CONFLICT`. SUCCESS and TERMINAL are each
bound through the exact claim/marker lineage and simultaneous valid terminal classes fail closed.
An ineligible approval never turns any of those observed states into a fabricated absence.
The actual public qualification path creates a missing V9 claim parent through the durable
exclusive no-replace primitive, trusted-reads the claim, publishes and trusted-reads the marker,
and grants mutation eligibility to exactly one qualification process. The qualification effect
port performs no Docker, PostgreSQL, migration, candidate, production, or canonical mutation.

Process loss before durable claim publication preserves the same exact V9 invocation identity; it
does not issue a new Human invocation or create a retry identity. Because the exact approval,
authorization, run ID, and attempt ID are deterministic repository authority and neither claim nor
marker exists yet, one fresh process may continue that same invocation through exclusive claim and
marker publication. A single-contender qualification reaches mutation eligibility exactly once.
When two fresh recovery contenders race after the same pre-claim loss, the no-replace claim and
marker primitives admit exactly one mutation-eligible winner; the loser remains ineligible. Run ID,
attempt ID, replacement-attempt count, and automatic-retry count remain unchanged or zero as
applicable. After a claim exists, continuation to mutation eligibility is forbidden and recovery is
read-only reconciliation of the original attempt.

The actual V9 container started at `2026-08-17T03:53:43.488793181Z`. Its terminal receipt was
durably published at `2026-08-17T03:53:43.647Z`, before the official-image init sequence reached
final readiness at `2026-08-17T03:53:44.703180939Z` (approximately 1,215ms after start). The
terminal classification remains `POSTGRES_STARTUP_FAILED`; the probe-by-probe primitive result was
not durably preserved, so its historical diagnostic classification is
`READINESS_TRACE_NOT_DURABLY_AVAILABLE`.

The first load-bearing divergence was nevertheless finite: the REAL PostgreSQL worker preserved
only `error.code`. The selected `pg` client can produce the exact startup disconnect message
`Connection terminated unexpectedly` without a system code or SQLSTATE when the authoritative
Docker-published endpoint accepts and closes during startup. Normalization therefore emitted
`null`, and the readiness classifier returned `POSTGRES_READINESS_PROCESS_FAILURE` immediately
instead of waiting 500ms inside the same container/invocation. The repair maps only that exact
message to `PG_CLIENT_CONNECTION_TERMINATED_UNEXPECTEDLY`; all other absent-code or malformed
process results remain terminal. The fixed 60,000ms/500ms/120-attempt policy and 65,113ms outer
effect fence are unchanged.

Read-only reconciliation proved PostgreSQL major 17, no reference principals, no minimal bootstrap,
no migration history or target objects, no candidates, and no approved expected-catalog authority.
Exact compensation then stopped and read back the V9 container as exited, removed only the exact
V9 container, volume, and network, and independently verified all three absent. Claim, marker, and
terminal receipt bytes remain immutable. V9 zero residual is true, V9 retry remains forbidden, and
any later reference execution requires a fresh separately approved authorization revision; none is
created by this repair.

## V11 consumed history, V12 exhausted history, and V13 public-bootstrap qualification

V11 is historical `OUTCOME_UNKNOWN`, consumed, non-runnable, retry-forbidden, and compensated to
zero Docker residual. Its exact claim and marker remain present and immutable under the `v11`
path; SUCCESS and TERMINAL remain absent. V11 cannot be proposed, approved, or invoked again.

V12 is exact approved history with one issued Human invocation. Its Human allowance is exhausted,
durable consumption was not reached, retry is forbidden, and its claim, marker, SUCCESS, and
TERMINAL paths below `artifacts/day150/prefix-expected-catalog/reference-runs/v1/v12/` remain absent.
Its approval record is preserved and cannot be selected as fresh invocation authority.

V13 is the sole active finite Gate17 descriptor and is now non-runnable after one successful
Product Owner-approved execution. It retained the unchanged minimal scope, PostgreSQL 17 image,
exact five migrations and candidates, 89-statement privilege envelope, readiness policy, cleanup
policy, and zero production/canonical/B2/Gate2 operation limits. Its Human invocation issuance,
claim, consumption marker, pre-cleanup evidence, and SUCCESS receipt are present below
`artifacts/day150/prefix-expected-catalog/reference-runs/v1/v13/`; its TERMINAL receipt is absent.
The five generated candidate files retain their original unapproved bytes as historical source
artifacts; the exact five repository authority bindings are now Product Owner approved.

The real historical registry contains exact V7 opaque history, V8 exhausted history, V9 terminal
history, exact consumed V11 approval history, exact exhausted V12 approval history, and the exact
consumed successful V13 approval history simultaneously. V10 is represented by its explicit stale
non-runnable request authority and is not inferred runnable from revision order. Historical
qualification fixtures that made V13 selectable only inside an isolated repository remain test
evidence and do not make consumed V13 runnable again.
The canonical parser, repository loader, exact candidate selector, and public gate select exactly
one V13 approval without latest-wins or single-record semantics. Missing, stale, wrong-scope,
wrong-lineage, duplicate, and conflicting V13 records fail closed. The same executor path publishes
and trusted-reads the V13 Human invocation issuance, claim,
and marker in an isolated qualification repository and admits exactly one mutation-eligible process;
the qualification performs no Docker, PostgreSQL, migration, candidate, production, or canonical
operation.

The public catalog bootstrap obtains authorization ID, revision, source digest, plan/run/attempt
lineage, descriptor digest, and durable paths from one validated active execution binding. It passes
that binding's approval candidate to the canonical repository loader; it does not independently scan
raw approval records or encode a current-revision authorization literal. Mixed historical/current
identity, V12/V13 identity, V13/V12 path, unknown revision, missing descriptor, historical V9,
V11 or V12, and stale V10
cases all fail closed before external mutation.

The permanent V9 readiness regression executes the generated REAL PostgreSQL child program itself.
Only `ECONNREFUSED`, `ECONNRESET`, `EPIPE`, PostgreSQL `57P03`, and the exact normalized
`PG_CLIENT_CONNECTION_TERMINATED_UNEXPECTEDLY` startup disconnect are transient. Arbitrary no-code
errors remain terminal. The full 21-case matrix retains the 60,000ms window, 500ms minimum spacing,
120-attempt maximum, zero authorization retry, and zero container recreation.
