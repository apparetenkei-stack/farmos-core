# Spec Kit Core Qualification Report — Day150.5-A

Result: `QUALIFICATION_PASS_FINAL_A_REVIEW_PENDING`.

Qualification completed at `2026-08-21T05:10:00Z`; this evidence was reconciled at `2026-08-21T06:40:52Z`. The original evidence acquisition timestamps remain unchanged.

The Product Owner approved GitHub Spec Kit at upstream `github/spec-kit`, package `specify-cli`, executable `specify`, version `v0.16.5`, release commit `87a0e9715d59079e46b44208ba009bf1a6976643`, and Codex integration key `codex`. Read-only tag dereference established annotated tag object `b0f18655e874825b7c4f7937c9777eab8d38928`, release commit `87a0e9715d59079e46b44208ba009bf1a6976643`, and source tree `47707d517b074d2388c924e1b664c7c194d3dff6`.

The official Astral installer was downloaded at the pinned `uv 0.12.5` endpoint and fingerprinted `sha256:504511fbbbd811aeaba6738abc79408956b6c7da0ca35437b3dcc24a41efc111`. It was installed only under `/Users/hayate/.local/share/farmos-ef1-spec-kit/uv-0.12.5/bin` with `UV_UNMANAGED_INSTALL` and `UV_NO_MODIFY_PATH=1`. Installed `uv` reports `0.12.5` and has `sha256:ad3564874e19defa0debefcf48e8381ac1d087c584190c1323c247bd351dd25f`. Historical qualification evidence establishes unchanged shell-profile readback and no created or changed profile; it did not retain individual checked paths or before/after hashes. Those bindings are therefore recorded as unavailable rather than invented. The active A authority does not require exact historical per-profile hashes.

That uv installed `cpython-3.11.16-macos-aarch64-none` only under `/Users/hayate/.local/share/farmos-ef1-spec-kit/python-3.11.16`. Its `python3.11 --version` readback is `Python 3.11.16`; executable fingerprint is `sha256:6b97988de3d1ec7e03924cd2367e5a5b188699d518eed751c1c8c38c70811e5f`. Apple system Python `3.9.6` was not changed or used for the tool environment.

`specify-cli` was installed from the exact approved Git commit into the isolated uv tool root, with executables in `/Users/hayate/.local/share/farmos-ef1-spec-kit/bin`. `specify --version` and `uv tool list` both return `0.16.5`; `specify version` reports Python `3.11.16`, Darwin, and arm64. The installed `direct_url.json` identifies `https://github.com/github/spec-kit.git` and commit `87a0e9715d59079e46b44208ba009bf1a6976643`. Distribution tree fingerprint: `sha256:929574077d44c9e5145e58719888a15ab48f017e18a9481316d3d59e6f33de40`; entrypoint fingerprint: `sha256:42b3c24cc7d1253b93cdbc52cbee08ca38323cf9859ad7dfd6caf214c169bc9e`.

`specify --help` and `specify integration --help` established the available command and integration subcommand surfaces. The installed Codex integration module has `sha256:121c9e59466576caaf25a05aa18da1a09de8582e79415ae025b6ccf50675525d` and binds `key = "codex"`; exact release source confirms the same. `specify integration list`, `info codex`, and `status` were attempted only in a fresh temporary non-project directory; all fail closed with `Not a Spec Kit project (no .specify/ directory)`. No repository initialization was performed. In particular, `specify init`, integration installation/switch/use, Homebrew, sudo/root, credentials, production connections, DB/runtime mutation, feature-code changes, and external inference activation were not used.

The active v1.1 A requirement that Spec Kit Core Qualification be established is now satisfied. Day150.5-A is not complete: its single remaining exit condition is the independently required final Sol review, which must assess the completed qualification under the active A contract. This qualification task does not run that review, stage files, create a commit, push, or start Day150.5-B.
